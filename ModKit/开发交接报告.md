# Flotsam ModKit 开发交接报告

> 目的：让新开会话的 agent 能直接接力开发，不需要重新逆向或重新踩坑。
> 最后更新：2026-09-21（对应已安装到游戏的版本：host api 1.1，八个 mod 见下文版本号）。
> 配套文档：`ModKit/设计与实现方案.md`（架构设计）、`ModKit/使用说明.md`（玩家视角用法）、
> `_mod_recon/native_map_recon.md`（原生世界地图逆向结论）。

---

## 0. 环境与技术栈

| 项 | 值 |
|---|---|
| 游戏 | Flotsam 1.0.1f7，Unity 6（6000.3.16f1，Mono） |
|  mod 加载器 | BepInEx 5.4.23.5（Mono），HarmonyX |
| 游戏目录 | `F:\Game\Flotsam` |
| 编译目标 | `netstandard2.1`，C# 9，**无 NuGet**，全部 `<Reference>` 指向游戏 `Flotsam_Data\Managed` 与 BepInEx `core` |
| 构建/安装 | `ModKit\build.ps1`（默认 build+install；`-NoInstall` 只编译） |
| 运行日志 | `F:\Game\Flotsam\BepInEx\LogOutput.log`（每次启动覆盖；agent 可直接读） |
| 原生素材清单 | `F:\Game\Flotsam\Mods\nativeskin_names.txt`（每次进游戏时导出） |
| 反编译参考 | `_mod_recon\src\Assembly-CSharp.decompiled.cs`（300,874 行，行号在下文引用） |
| 视频抽帧 | 本机有 ffmpeg：`C:\ProgramData\chocolatey\bin\ffmpeg.exe`（用户给视频路径时可抽 PNG 再用 Read 看） |

**重要：改代码后必须重启游戏才生效。** Mono 下程序集常驻，F10 管理器的「重载」只重跑 mod 生命周期（OnDisable/OnEnable），不会重新加载 DLL。

---

## 1. 仓库结构

```
ModKit/
  build.ps1                     构建+安装
  设计与实现方案.md / 使用说明.md / 开发交接报告.md
  specs/                        新 mod 的设计文档与实现计划（2026-09-20 批量管理+一键电网）
  tools/EnergyPlanner.Tests/    纯算法测试控制台（net8.0，`dotnet run --project` 即跑，13 项；改 EnergyPlanner.cs 必跑）
  src/
    Flotsam.ModKit.Abstractions/   IFlotsamMod、IModContext、IUiService、IKeybind…（纯契约）
    Flotsam.ModKit.Game/           与游戏类型打交道的唯一层 + 通用 UI  kit
        GameApi.cs        GameMovement.cs   GameWorld.cs      GameProduction.cs
        GameBuildings.cs  GameKeys.cs       GamePatches.cs    GameUi.cs   NativeSkin.cs
        GameBatch.cs      GameEnergy.cs     EnergyPlanner.cs（纯连线算法，零 Unity 依赖）
        GameAssignments.cs（小人技能→任务优先级重排 + 自动加技能点，纯 public API 零反射）
    FlotsamModKit.Host/            唯一 BepInEx 插件：发现/生命周期/配置/按键/补丁/UI 服务/管理器
        ModKitPlugin.cs ModKitRuntime.cs ModEntry.cs UiService.cs ManagerWindow.cs
        ConfigService.cs KeybindService.cs PatchService.cs EventService.cs ManifestReader.cs
    mods/
      FlotsamMod.KeyboardMove/   KeyboardMoveMod.cs + SailHud.cs
      FlotsamMod.MiniMap/        MiniMapMod.cs（含 MiniMapView）
      FlotsamMod.MaterialHelper/ MaterialHelperMod.cs + MaterialPanel.cs（含 Driver/Hook/Badge/Popup）
      FlotsamMod.BuildingFinder/ BuildingFinderMod.cs
      FlotsamMod.GameplayTweaks/ GameplayTweaksMod.cs + TweaksPanel.cs（平衡滑块面板）
      FlotsamMod.BatchManager/   BatchManagerMod.cs + BatchPanel.cs（同型号批量勾选/操作）
      FlotsamMod.PowerLink/      PowerLinkMod.cs（一键自动连电网，无窗口）
      FlotsamMod.AutoPriority/   AutoPriorityMod.cs（自动任务优先级+自动加点，无窗口）
Mods/<id>/                       安装产物：mod.json(人工维护) + config.json + keybinds.json + <mod>.dll
```

### 生命周期契约（api 1.1）
`OnLoad → OnEnable → (OnTick 每帧) → OnDisable → OnUnload`，另有 `OnGameStart / OnGameEnd`（**host v1.0.1+ 由 `ModKitRuntime.PollGameSession` 轮询 `GameApi.IsPlaying` 边沿派发，不走 GameEventDispatcher**）。
**重大坑（host v1.0.1 修复）**：游戏每次场景加载开头都会 `GameEventDispatcher.RemoveAllGameEventListeners()`（`LoadingScreen.LoadSceneCoroutine`，decompile 105273）——host 与各 mod 在启动期（主菜单 OnEnable）注册的所有事件订阅**一进存档就全灭**。后果曾是：OnGameStart/OnGameEnd 从未送达过任何 mod、各 mod `Events.On` 全部失效（batchmanager/powerlink/buildingfinder 靠"开窗刷新/心跳/OnTick"兜底才没暴露；autopriority 纯事件驱动直接暴露）。修复=轮询边沿派发 + 上升沿先 `EventService.RestoreAll()`（逐条 Remove+Add 幂等重注册）再派 OnGameStart。**mod 侧 `Events.On` 用法不变，但别再指望 boot 期订阅能活过读档——host 会替你重注册。**
**mod 类不是 MonoBehaviour**：每帧逻辑要么用 `OnTick`（host 在 `ModKitRuntime.Update` 里泵），要么自建 driver GameObject（如 SailDriver、MaterialHelperDriver）。
api 1.0 → 1.1 的唯一变化：`IFlotsamMod` 增加 `OnTick()`（`FlotsamModBase` 有虚默认实现）。

---

## 2. 通用 UI kit（GameUi / NativeSkin / UiService）

### 2.1 NativeSkin（运行时采集原生素材）
- 进存档后从 `GameManager.UIManager.Canvas` 子树扫 `Image`/`TMP_Text`，按**精确名字表**取素材，取不到退化为子串匹配，再退化为程序化纯色。
- 当前精确名字（已验证可用）：
  - 面板体 `UI_Panel_Generic_Rounded_32x32`；描边 `UI_Panel_Generic_Border_Rounded_32x32`
  - 标题栏 `UI_Panel_Generic_Rounded_Top_32x16`（薄荷色 tint `(0.55,0.88,0.75)`）
  - 按钮 `UI_Button_Generic_Medium_Background_64x64` + pressed `..._pressed_64x64` + 描边 `UI_Button_Generic_Medium_Border_64x64`
  - 关闭 `UI_Icon_InfoPanel_Close_64x64`（off: `..._Close_off_64x64`）；拖拽把手 `UI_Icon_InfoPanel_Drag_64x64`
  - 开关图标 `UI_Icon_InfoPanel_On_64x64` / `..._Off_64x64`；定位 `UI_Icon_InfoPanel_Locate_64x64`；加减 `..._Plus/Minus_64x64`
  - 凹槽/列表底 `UI_Background_Generic_SoftSquare_84x84`、`UI_Bar_Rounded_80x80`
  - 玩家标记圆环 `UI_Bar_RadialCircle_Border_256x256`
- 颜色（取样自游戏截图）：面板纯白、描边近黑紫 `(0.14,0.12,0.19)`、文字深青黑 `(0.15,0.21,0.19)`、行背景不透明灰绿 `(0.86,0.90,0.88)/(0.93,0.96,0.95)`、危险红 `(0.87,0.30,0.26)`、成功绿 `(0.20,0.62,0.30)`。
  **行背景必须不透明**：白底上加半透明叠色会"看不见"，曾被用户报为"对比度低"。
- 字体：采集两个 TMP 字重（`Font` 正文 / `FontBold` 粗体），偏好顺序 body=`Regular/Book/Light/Medium`、bold=`Bold/SemiBold/DemiBold/Medium/Heavy`；当前游戏里正文为 `NotoSansSC-Regular SDF`。
  `GameUi.Label(..., bold:true)`（默认）优先用原生粗体字重，没有才 TMP 合成粗体。**游戏 UI 整体是粗字重，正文用 regular 会被用户认成"字体不对"**。
- 场景切换会销毁采集到的 Sprite/Font：`ModKitRuntime` 在 GameEnd 调 `UiService.ResetSkin()`→`NativeSkin.Reset()`+`GameUi.ResetFont()`；各 mod 窗口在 GameEnd 销毁、下次进存档惰性重建。
- 日志行 `[modkit] native skin: …` 含 sprite 数、font/bold 名、全部字重清单；完整名字表写 `Mods/nativeskin_names.txt`。

### 2.2 UiWindow（所有 mod 窗口的统一外壳）
- 白色九宫格面板 + 描边 + 薄荷标题栏（标题居中、左侧拖拽把手图标、右侧 **[提示开关][红色✕]** 两枚图标按钮，✕ 在最右角）。
- 拖拽标题栏移动；**双击标题栏复位位置+尺寸**；右下角 `UiResizeHandler` 握柄改尺寸。
- 位置/尺寸/提示开关全部按 `configKey` 记忆：`<key>.x/.y/.w/.h/.tips`。
  **高度存的是"含提示"的基准高度 `_canonicalH`**，显示高度 = 基准 −（提示隐藏 ? `TipsHeight` : 0）。这样恢复一个"提示已隐藏"的窗口也能正确收起。
- `TipsHeight` + `SetTips(params RectTransform[])`：隐藏提示时窗口回收高度并触发 `TipsChanged(bool)`。
  **各 mod 必须自己把"提示占的那块布局"也收起来**（否则留空白带）：
  - SailHud：Footer 容器（按钮锚 Footer 顶边），`TipsChanged` 改 Footer 高度 78↔26。
  - MaterialPanel：`TipsChanged` 移动摘要行（-30↔-2）并改滚动列表 `offsetMin.y`（52↔24）。
  - MiniMap：`TipsChanged` 改控制区高度 116↔30（窗口高度由 UiWindow 回收 86）。
- `SetClickThrough(bool)`：给 Root 加 CanvasGroup 关 raycast。**钉住原生悬浮框期间必须把材料助手面板设为 click-through**，否则面板会吞掉点向悬浮框材料格的点击（这是"点缺料没反应"的历史根因）。
- **置顶开关**：标题栏第三枚按钮（定位图标，亮=置顶/暗=沉底），按 `<key>.top` 记忆，**默认沉底**。沉底不能只改 sortingOrder：mod 画布是 Screen Space - Overlay，overlay pass 恒画在所有相机渲染（含原生 Screen Space - Camera UI 画布）之后。故沉底时镜像原生主画布（`GameManager.UIManager.Canvas`）的 renderMode/worldCamera/planeDistance/layer 并把 sortingOrder 降到 `原生order − 1000 + 本窗口order尾三位`；置顶恢复 Overlay + 原 order（`UiWindow.CaptureCanvasOrders/ApplyTopMost`，日志行 `[modkit] window '...' topOrder=... nativeCanvas=...`）。副作用：沉底窗口在打开世界地图期间不可见（原生 UICamera 被关）。HUD 按钮与 Toast 仍常驻 Overlay 顶层。
- `ContentMode`：`Uniform`（Body 按默认尺寸布局后整体等比缩放，默认）/ `None`（Body 拉伸铺满，mod 自己在 `Resized` 里重排；小地图用这个）。

### 2.3 UiService（HUD 按钮 + Toast）
- HUD 按钮：原生按钮皮肤；同锚点自动错位堆叠；`AddHudButton(id,label,onClick,anchor,config,key)` 自动可拖拽+记忆（`<key>.x/.y`）+双击复位。
- **Reskin 与 EnsureSkin 闩锁解耦（host v1.0.1 修）**：按钮的圆角原生皮肤靠 `Tick` 里 `_buttonsReskinned` 独立标志批量 Reskin，**不能挂在 `EnsureSkin()` 的一次性返回值上**——OnGameStart 正常派发后，mod 在 OnGameStart 里的 Toast 会先闩锁 harvest，`Ui.Tick` 的 `EnsureSkin()` 恒 false → Reskin 永不执行（症状=按钮直角白块但图标正常，因图标走 mod 自己的 `NativeSkin.Find`）。`ResetSkin` 同时清两个标志；闩锁后新建的按钮在 `AddHudButton` 里立即 Reskin。
- Toast：浅色面板+描边，右侧 ✕ **立即移除并让下方条目马上上移**；4s（错误 6s）自动淡出。

### 2.4 GameUi.SliderRow（原生皮肤滑块，v1.1 新增）
- `GameUi.SliderRow(parent,label,min,max,value,onChanged,onCommit,labelWidth,valueWidth,trackHeight,size,step)` 返回 `UiSliderRow`：`标签 [----o----] [数值]%' 一行，凹槽用 `SlotSprite`、填充用 Accent 纯色、手柄用 `ButtonSprite`/`DragSprite`、数值框用 `GameUi.NumberInput`（原生皮肤 `TMP_InputField`，`ContentType.IntegerNumber`）。
- **不用 UGUI `Slider`**（fill/handle 锚点难对齐），改用自建 `SliderDrag : IPointerDown/Drag/UpHandler`，直接按 `Track.rect.width` 摆 fill 宽与 handle 位；`ScreenPointToLocalPointInRectangle` 取点，故在 Uniform 缩放下也准。
- **数值可拖动、也可键入**：数值框 `onEndEdit`（回车/失焦）→ `SliderDrag.ApplyInput` 解析→钳制→吸附→`OnChanged`+`OnCommit`；拖动滑块时若数值框未聚焦则反向写回文本（`UpdateVisuals` 里 `!isFocused` 守卫，避免和玩家抢输入）。
- **档位吸附**：`SliderDrag.Step>0` 时 `SetValue` 走 `Quantize`（`Round(v/step)*step`），拖动与键入都吸附（gameplaytweaks 用 `step=5` → 5% 一档）。
- `onChanged` 拖动中实时触发（改数值），`onCommit` 松手/键入确认才触发（存盘/写日志）——避免拖一下刷屏日志、刷爆 config。
- **键入态**：数值框聚焦时经 `InputFocusRelay` 调 `GameUi.PushTyping/PopTyping`（引用计数，`UIManager.SetState(UIState.Typing)`），让快捷键与镇心移动忽略键入；面板销毁要调 `GameUi.ResetTyping()` 兜底（聚焦中被销毁不会漏 Pop）。注意 BatchManager 的 `BatchPanel` 有自己的同名私有 `PushTyping/PopTyping`，与 GameUi 的静态版互不相干。
- `SliderDrag.OnRectTransformDimensionsChange` 会在轨道首次布局/每次尺寸变化时重算填充与手柄位置：**没有它，首帧 `Track.rect.width=0`，手柄会一直贴在左端直到用户第一次拖动**（已踩坑）。
- 当前唯一使用者：`flotsam.gameplaytweaks`。

---

## 3. 八个 mod 的现状

### 3.1 flotsam.keyboardmove「键盘航行」v3.0.0
- 按键：`W/S/A/D` 进退转向（默认取游戏原生镇心绑定），`Return` 立即靠港，`F8` 暂停。
- 配置：`speedMultiplier`、`commitDistance`(250)、`commitRotation`(90)、`showHud`、`verbose`、`drivingEnabled`、`hud.*`、`button.*`。
- 机制（`SailDriver` + `GameMovement`）：
  - **虚拟航程**：不激活 WorldMap；每 FixedUpdate 用 `WorldMap.MovementSpeed/RotationSpeed` + `Engine.ReturnMoveableDistance` 自算位置/朝向（转向独立于前进，修过原版 early-out 导致不能原地转的坑）。
  - **单次提交**：松手或超过距离/角度阈值才 `MovementEvent.DispatchTownheartMove` 一次（= 原版关地图的那一次）。高频提交会让 `World.RepositionTownheart` 反复 despawn/respawn 全部地标 → "建筑消失"。
  - **地图移交**：航行中按 M 开地图 → 把虚拟位置 `SyncMapTownheart` 一次后停止自提交，由原版地图关闭时自己提交；`Settle` 拒绝在地图打开时运行。（修过"开/关地图后物质漂移"。）
  - 提交前 `World.UpdateTiles` 补瓦片 + 边界钳制，避免 `IsPositionInWorldBounds` 拒绝。
  - 能量：每步 `Engine.ConsumeEnergy`；冷却中 `ReturnMoveableDistance=0`。
- 「航行仪表」窗口：状态/航向/速度/本段/累计/电量可航/引擎 + 立即靠港/暂停按钮 + 可隐藏提示；被阻挡时直接显示原因（含 `GameApi.MovementBlockerCount` 排除地图自身 blocker 的逻辑——**关过一次地图后 `IsTownMovementBlocked` 永久为真**，这是"键盘移动失效"的历史根因）。
- 已知历史根因备查：`WorldMap.Deactivate` 结尾 `AddMovementBlocker(this)`（decompile 159742）。
- **移动保护（对齐世界地图碰撞）**：`GameMovement.BlocksMovement(from,to,voyageBase,out why)` —— 点在多边形内测试两组来源：`Buildable.BlockingPolygons`（玩家建筑/系泊阻挡，与建造重叠检测同一集合）+ 各地标 spawner 的 `TileSpacePolygon`（岛轮廓；测试点按 `WorldPosition2D - TilePosition` 反偏移；**岛没有 Obstacle 组件，`Obstacle.AllObstacles` 和 `WorldMap.Obstacles` 都指望不上**）。拒绝"从多边形外跨进内"的步：手动航行 Blocked「离建筑/地标太近」，自动航行取消+提示；起点或当前点已在多边形内的豁免（镇心自身平台/已卡住时能开出）。

### 3.2 flotsam.minimap「常驻小地图」v3.1.0
- 按键：`M` 开关，`Home` 回镇心并跟随。
- 配置：`visible`、`followTownheart`、`viewRange`(1400)、`refreshHz`(10)、`showFog/fogInvert/showIcons/showTown`、`iconSize`(20)、`iconMinAlpha`、`window.*`、`button.*`、`schema`。
- 视图：`MiniMapView`，**非方形贴图铺满窗口可用区**（缩放比例取短边；改窗口尺寸会重建贴图，真正改变视野而不是放大马赛克）。
- 图层：海面= `WorldTile.WorldBounds` 矩形（teal `(56,138,156)`）+ 边线；迷雾= `WorldTile.FogOfWarAlphas` 真实战争迷雾（深一档 `(14,32,44)`）；游泳圈白环；
  我的建筑= 贴图内 7px 描边/5px 亮芯小点（**不要用投影方块或建筑图标：图标多为 null、投影会糊成黑块**）；
  地标/兴趣点/漂浮物= 原生 `ISpawner.Icon` 的 UGUI Image 池；
  玩家= 白圈 + **128px 逐像素抗锯齿箭头**（尺寸 = `clamp(iconSize*0.75,12,22)*1.5`）+ 深色外描边，航行中变绿。
- 刷新：贴图按 `refreshHz` 重绘；玩家标记每帧更新（跟随键盘航行的未提交位置 `GameMovement.DisplayPosition`）。
- 交互：滚轮缩放（以光标为中心）、左键拖拽平移（取消跟随）、**左键单击=自动航行到该点**（`GameMovement.AutoActive/AutoTarget/AutoInput` 自动驾驶层，SailDriver 消费：转向→对准推进→60u 内刹车、18u 内靠港提交；方向键接管、右键/双击/开地图/被阻挡取消；目标点画薄荷十字环）、双击居中（取消自动航行）、右键回镇心并取消自动航行。
- **采完的资源不再显示**：三通道过滤（`GameWorld.IsDisposed/IsDepleted`）——POI 用世界地图自己的判据 `PointOfInterestSpawner.ReturnSpawnerCount()<=0`（`WorldMapFlotsam.OnSalvage` 就是据此销毁标记的；反射成分清点保留为后备）；地标监听 `GameEventType.LandmarkDisposed`（spawner 本身仍留在 tile 列表里）。换存档自动清空 disposed 集合。
- 历史根因备查：旧版 `OnGameStart` 调 `FitWorld()` 把跟随关掉并缩到全世界且写进 config → "小地图不刷新"；已有 `schema=2` 迁移自动修复。

### 3.3 flotsam.materialhelper「材料助手」v4.3.0
- 按键：`G`（按住+点击=补齐；单按=只打开面板）、`H` 开关面板。
- 配置：`zoomLevel`、`chainDepth`(2)、`showPanel`、`pinTooltips`、`hookNativeCells`、`verbose`、`panel.*`、`button.*`。
- Harmony 补丁（`OnEnable` 注册，日志会打印每条）：
  | 目标 | 类型 | 作用 |
  |---|---|---|
  | `RecipeDisplay.UpdateDisplay` | postfix | 给工坊配方材料格挂 `IngredientRowHook`（左键补齐/右键定位） |
  | `ProductionPanelRecipeToggle.Initialize / UpdateState` | postfix | 挂/刷新配方行「缺 N」徽标（仅展示） |
  | `BuildableTooltipItemSlot.Initialize(CountedItemProperty,bool)` | postfix | 挂 `ConstructionSlotHook`（建造悬浮框材料格可点） |
  | `BuildableTooltip.HideTooltip` | **prefix** | 钉住期间禁止建造悬浮框自隐藏（它直接 `SetActive(false)`，绕过 TooltipPanel） |
  | `TooltipPanel.Update` / `HideTooltip_Internal` | prefix | 钉住期间禁止物品悬浮框跟随/隐藏 |
- **交互映射（当前设计，用户确认过的目标行为）**：
  1. 点击要造的建筑（建造栏条目）→ 钉住建造悬浮框 + 材料助手显示该蓝图需求（`CaptureBlueprint`，含 1.2s 重试等格子生成）；
  2. 钉住后点击缺的材料 → **只定位**（飞向并打开生产它的建筑面板）；不能生产的弹「如何获得」（扫描 `LandmarkInteractableWithComposition.Composition` 给打捞来源）；
  3. 按住 G 点要造的建筑 → **直接补齐整条链**（`AimDetect`：逐项 `ScheduleBatches` + 递归 `FillChain`）；
  4. 点悬浮框以外 → 解除钉住；离开正常游戏状态（菜单/地图/对话）自动解除；
  5. 钉住期间材料助手面板 click-through，避免吞点击。
- 面板：跟随打开的建筑面板（工坊=选中配方材料；建造中=建造材料）或手动蓝图模式；每行 图标/名称/库存·需求·在途/补齐/定位；「一键补齐」= 手动模式逐行补齐，否则补当前配方整链。
- 排产 API：`GameProduction.ScheduleBatches / FillChain / FindProducersFor / CandidateSnapshot / IncomingCounts / SalvageSources`（后两个是"一次遍历"快照，避免每行扫全部生产者）。
- 日志（`verbose`）：补丁注册、材料格挂钩、pin 状态迁移、`resolve:` 定位结果、`aim-fill:` 每条排产、`capture:` 蓝图需求清单。**排错先读这些行。**

### 3.4 flotsam.buildingfinder「建筑总览」v3.0.0
- 按键：`B`。配置：`zoomLevel`、`hideAfterFocus`、`includeUnfinished`、`groupAllByCategory`、`maxRows`、`lastCategory`、`window.*`、`button.*`、`schema`。
- 左侧原生分类（图标+配色+数量），右侧按游戏排序列建筑（图标/名称/建造中/故障数），搜索框（输入时推 `UIState.Typing`）。
- 分类列表必须在 `OnGameStart`/首次打开时构建（`GameManager.Settings` 启动时为空）。

### 3.5 flotsam.gameplaytweaks「游戏性调整」v1.0.0
- 按键：`F7`（可重绑，`AllowWhilePaused`）开关面板；HUD 左上角「游戏性调整」按钮同效。
- 面板：`UiWindow`（Uniform 内容模式）+ `ScrollList` 中段 + 顶部「总开关/恢复默认」+ 底部帮助（作为 Tips，可隐藏并回收高度，逻辑仿 MaterialPanel）。9 个 `GameUi.SliderRow` 分五区（重量/发电/小人/船只/美观度），**数值既可拖动也可直接键入**（`NumberInput`，回车/失焦生效），**档位 5%**（`step=5`，拖动与键入都吸附到 5 的倍数）。窗口可拖动、可缩放、位置尺寸记忆（`panel.*`），按钮位置记忆（`button.*`）。
- 配置（均为「原版百分比」整数，默认 100）：`active`、`weightEnergyPct`(0-300)、`tugCapacityPct`(100-1000)、`generatorPct`(0-500)、`agentEfficiencyPct`(25-500)、`boatSpeedPct`(25-500)、`beautyNegBuildPct`(0-200)、`beautyPosBuildPct`(0-500)、`beautyNegMoralePct`(0-200)、`beautyPosMoralePct`(0-500)、`showPanel`、`verbose`。
- 机制：**9 条 Harmony postfix** 全部读 `GameplayTweaksMod` 的静态乘数、改写游戏自己的返回值；`Active=false`（总开关关）时全部 early-return 回原版；禁用 mod 时 host 撤销补丁亦回原版。滑块拖动/键入只改静态字段（实时生效），**松手或键入确认才 `Config.Set+Save`+写一行日志**（降噪、免刷盘）。
  | # | 目标 | 语义 |
  |---|---|---|
  | 1 | `GameplaySettings.ComputeEelsPerUnit(float)` | 重量能耗：`base + (result-base)*pct`，base=f(0)（反射调用原方法+`[ThreadStatic]` 守卫防递归，按 `__instance` 缓存，GameStart/End 重置）。0%=重量不再增能耗 |
  | 2 | `Engine.ReturnTownTugCapacity()` | 载重上限 `*pct`：抬高牵引上限 → `TownAvailableTugCapacity` 变大 → 超重也能继续建（不改 TownWeight，与 #1 独立） |
  | 3-5 | `EnergyPassiveGenerator/EnergyItemProducer/EnergyManualProducer` 的 `get_Production` | 发电效率 `*pct`（三类共用一条 postfix；`EnergyGrid.ReturnEnergyProduction` 求和 Production，故实际入网电量随之放大） |
  | 6 | `DrifterAttributes.ReturnAttributeModifier(Attribute)` | 小人整体执行效率 `*pct`：该函数是全部干活速度（排产 136821/建造 134162/研究 27019/发电 21537/通用 134701）与走路（Athletics→`Navigator.ReturnSpeed` 115270）的公共乘数；`(AttributeType,int)` 重载（9305）仅 tooltip 文案不碰。下限 25% 防小人冻住 |
  | 7 | `Navigator.ReturnSpeed(TerrainType)` | 船速 `*pct`：船的 Navigator 是 `_isVessel` 且 `_agent==null`（114792-114799），ReturnSpeed 只返回固定地形速度、**吃不到小人效率**（故"小人快了船还慢"）。**航行中船的地形是 WaterSurface**（`UpdateGraph` 按水图决定地形，Vessel 只是闲置登船船长）——首版误用 `terrain==Vessel`  gating 导致完全无效（用户实测 25% 也无变化）；现改为水/Vessel 地形先筛、再确认 `_boat!=null` 才缩放，游泳小人（WaterSurface、`_boat==null`）不受影响 |
  | 8 | `Buildable.ReturnBeautyScore()` | 建筑美观分：负分 `*negPct`、正分 `*posPct`；改动后调 `Community.UpdateBeautyScore()` 重算缓存的 `BeautyScore` |
  | 9 | `TownBeautyMoraleEffect.ReturnModifier()`（命名空间 `PajamaLlama.Flotsam.Morale`） | 美观→士气修正：负 `*negPct`、正 `*posPct`；读取时实时缩放，无需重算 |
- 日志：OnLoad 打一行配置摘要；OnEnable 打 `9/9 patch(es) applied`（PatchService 每条补丁各打一行）；OnGameStart 提示就绪；每次滑块松手/总开关/恢复默认各一行。**不打每帧、不打拖动中的每一跳**。
- 已实测（进游戏读 `LogOutput.log`）：`discovered 10 mod(s)`、`10/10 enabled`、本 mod `loaded`+`9/9 patch(es) applied`（含 `Navigator.ReturnSpeed <- AfterBoatSpeed`），本 mod **0 error**（日志中 perfboost 的 hotspot 报错属并行 mod，与本 mod 无关）；用户已进存档确认滑块可拖动/键入且效果生效。并行 mod 与本 mod 共存无冲突。

### 3.6 flotsam.batchmanager「批量管理」v1.0.0
- 按键：**`Ctrl+1`**（KeybindService 不支持修饰键 → 注册 `Alpha1`，`OnTick` 里 `IsDown && GameKeys.GetCtrlHeld()`，沿 MaterialHelper 的 shift 组合先例；keybinds.json 只存基础键）。HUD 按钮「批量管理」（图标延迟到进存档首个 tick 才 SetIcon——主菜单时 NativeSkin 未采集，OnEnable 设会永远 null；`OnGameEnd` 会重置 `_iconSet`，采集精灵随场景销毁，二次进存档需重设）。
- 配置：`highlightChecked`(true)、`confirmThreshold`(5)、`sortByDistance`(false)、`includeUnfinished`(true)、`hideAfterFocus`(false)、`zoomLevel`(0.6)、`maxRows`(400)、`verbose`、`window.*`、`button.*`、`schema`(1)。
- 窗口（BatchPanel，UiWindow+Uniform）：顶部搜索/状态行（`显示 X / 共 Y 座建筑 · Z 个型号`）/工具条（**选可升级/全选/反选/清空**/已选计数/◀ n/m ▶）；左栏=固定「全部建筑」项+按原生分类分节的**型号列表**，**分类默认收起**（点分类头展开/再点收起，`_expanded` 集合；搜索非空时自动全展开、清空恢复原折叠态；折叠只重建左栏，不动右栏/勾选/跳转）；分类头=类别图标+类别色+`▶/▼ 分类名 (n)`，可点击（Button）。同型号 = 共享 `BuildableProperties` 引用（`GameBatch.GroupByType`；类型名走 `I2.Loc.LocalizationManager.GetTranslation(LocalizedNameTerm)`（I2 合并在 Assembly-CSharp 内），失败退 `Buildable.Name`）。
- 型号行徽标 **`可升 X/Y`**：X=`CanUpgrade()` 通过数（=原生三条件：已建成+科技解锁+**社区材料足够**），Y=`Properties.Upgrade != null` 的总数；另有 拆/升中、建造中、停用计数。右栏实例行=原生 CheckOn/Off 图标勾选框、图标、名称+状态后缀、⚠故障数、[定位]；**满足升级条件（CanUpgrade）的行名用成功绿 `GameUi.Good`**。全部建筑模式拼接所有可见型号并加「型号 · 」前缀；`maxRows` 截断加提示行。
- **「选可升级」快选**（全选左侧）：勾选当前右栏所有 `CanUpgradeNow` 的行（与全选同作用域=右栏），无符合项 Toast+日志；日志 `pick-upgradable: right=N eligible=K total=M`。全选 chip 文字随作用域变化（全部建筑=「全选」/型号=「全选本型号」）——全选/反选/清空/选可升级都只作用于**当前右栏列表**。
- **防选错三件套**：勾选即世界高亮（`Buildable.OutlineRenderer.UpdateSelectedObject/ResetHighlightOutline`，原生电缆 cursor 同款）+ 窗口可见期间 **2Hz 重刷**（原生 `OnDeselected` 17244 会清高亮，必须重刷）+ `◀▶` 跨型号逐个跳转（`_checkedOrder` 稳定点击序；跳转= `GameBuildings.Focus`）。高亮清除点：取消勾选/Hide/Destroy/GameEnd/OnDisable 全覆盖；`Destroy()` 必须先 `PopTyping()`（搜索框聚焦中退存档会把游戏卡死在 UIState.Typing）。
- 批量执行（`GameBatch.Run(targets, op, verboseLog)`）：逐个调原生同款方法——Salvage/CancelDeconstruction/Upgrade/CancelUpgrade/Activate/Deactivate（decompile 4156/4320/4246 同一逻辑），预检 `CanBeDeconstructed(out _)/CanUpgrade()/ShowActivationElement(17985)/ShowDurabilityElements(17997)/BuildPhase`，跳过原因计数进 `BatchOutcome` → Toast 汇总一行 + 日志一行；verbose 才有逐条 `op 名称 ok|skip:原因`。开关支持判定= `Properties.ShowActivationElement`。
- 拆除内联确认：勾选 ≥ `confirmThreshold` 时操作栏原位变红条（10s 超时自动退）；确认态退出途径=取消钮/超时/关窗/勾选变更/切型号/搜索/异 op 执行。**原生 PopUpDialog 无可自定义文案的通用确认 API（57323 起核实），别再找。**
- 刷新：`BuildableBuilt/Placed/Salvaged/Upgraded` 事件 → `_dirty`，下一 tick 且窗口可见才 Refresh（防事件风暴）；`OnGameStart` 强制 MarkDirty（读档后数据全新）；PruneChecked 用 Unity 假 null 剔除已拆建筑（`_checked`/`_checkedOrder` 同步）。**材料库存变化不触发刷新**——绿色行名/`可升 X` 可能滞后于材料变动，重开窗口或切型号即最新。
- **可诊断性（用户报过"全选不生效"而日志零痕迹后加固）**：面板 `Tick` 异常不再静默吞——`panel tick failed` Error 日志（1s 节流）；全选/反选/清空/选可升级**每次点击必留一行统计日志**（`selectall: right=N added=K total=M` 等），右栏为空时 Toast 明示而不是默默不动。排错先找这些行。

### 3.7 flotsam.powerlink「一键电网」v1.0.0
- 按键：**`Ctrl+2`**（同上调组合）。HUD 两钮：「连电网」（手动一键）+「自动连网:开/关」（切 `autoMode` 并落盘）。无窗口，全走 Toast+日志。
- 配置：`autoMode`(false)、`autoIntervalSec`(10)、`connectsPerTick`(4)、`optimizeExisting`(true)、`optimizeGainPct`(10)、`connectPolesToGrid`(true)、`mergePoweredGrids`(true)、`verbose`、`button.*`、`autobutton.*`、`schema`(1)。
- **算法全在 `EnergyPlanner.cs`（纯数据、零 Unity/游戏类型，`tools/EnergyPlanner.Tests` 13 项测试，改它必跑）**；游戏侧只在 `GameEnergy.cs`（快照 `EnergyGridManager.Grids→Links`、执行、复核）。管线：快照 → 整理决策 → **建操作队列（Remove 全部→Add→Merge）** → 汇总。
  - 带电判定：`IsTownheartGrid || ReturnEnergyProduction()>0 || ReturnStorageEnergy()>0`。
  - 增量：电网级 Prim（每轮取「一端带电一端不带电」的最短合法边，整网合并=游戏 `EnergyGrid.Connect` 的 Merge 语义）；平手规则=外侧端点空槽多 → Building 优先于 Pole → key 小（T12/T13 锁定）。
  - 整理（仅手动触发，自动模式永远只增量）：每个带电网内部 MST（防拆散自己）+ 向外 Prim；既有边 2% keep bonus 防翻动；`Gain=(可拆既有线长+增量新增)−重构树总长`，**孤岛旧线（两端均不在重构网络内）不计入 Gain**（否则纯孤岛就能虚报 100% 收益触发无意义重构，T11 锁定）；阈值 `max(可拆线长×gainPct%, 5u)`；Remove 仅限两端都在重构网络内的旧线（孤岛保护）。
  - `connectPolesToGrid=false` 时树上「叶子=杆」的边迭代剪除（杆只做接力）；默认 true 全接（顺便消杆自身「未连线」故障）。
  - **杆识别（修过误判）**：杆建筑的连接器也是 `EnergyGridBuildableComponent`，`is` 判断会把杆当 Building；正确判据=连接器同 GameObject 上 `GetComponent<EnergyGridPole>() != null` → Pole（`EnergyGridPole.Initialize` 20912 用 `GetComponent<EnergyGridConnector>()`，同物体实锤）；非建筑组件的装饰连接器也归 Pole。`GameEnergy.KindOf` 收口。
  - 智能互济：主体连完后，缺电网（`GridEfficiency<1`）×富余网（效率满且发电≥需求或有余电）之间取最短边合并，标志随并查集迁移，可连多对；两富余/两缺电不合并。
- **执行模型=分帧会话（崩溃加固后，替代旧的单帧 `RunAutoConnect`——该 API 已删除）**：`GameEnergy.BeginRun(...)` 只做快照+Plan+建队列并立即返回 `EnergyRunSession`；`PowerLinkMod.OnTick` 每 tick `Step(connectsPerTick)`（每条操作前重查 `Ready`，不 Ready 挂起下帧续；会话 >120s 未完成丢弃剩余队列+Warn）；Done 时 ReportRun（Toast 前缀/5s 去重/manual 强制逻辑）。每 tick ≤4 条 ≈ 手动逐根点线的节奏，避免单帧大量并网的异常游戏状态；代价=手动整理时拆/接跨几帧（短暂低电力窗口，可接受，取代设计文档的「同帧」承诺）。
- **抗硬崩溃 trace**：`Mods\flotsam.powerlink\trace.log`，每行 `File.AppendAllText` 立即落盘（硬崩溃时 Unity/BepInEx 日志缓冲全丢，这个不丢）：`run start(manual|auto, 队列统计)` → 每条 `pre connect/drop/merge A <-> B d=x` + `post ok|skip(原因)` → `run end <汇总>`；仅 manual 或队列非空才写（自动心跳空转零落盘）；>1MB 自动截断。**背景**：用户在「自动连网开+新建电池」时遭遇硬闪退（无托管异常、无 WER 记录、无转储，根因未定位）——若复发，trace 最后一行 `pre connect X <-> Y` 即肇事连接对；若崩溃时 trace 无进行中动作，则与连网无关（转查其他 mod/游戏本身）。
- 执行期每条边复核原生规则（`CanConnect()×2 / !IsConnected / 水平距<CableLinkRange`，完整复刻 `EnergyGridConnectCursorProperties` 90325/90417）后才调 `EnergyGrid.Connect/Disconnect`（static，39166/39182）——电缆可视、并/拆网、故障清除、**存档持久化**全是游戏自己的事件链，mod 零补丁、连线零成本（与手动一致）。重构干跑把槽位视为全空闲，被吸收孤岛的保留旧线可能占槽 → `LegalNow` 兜底、个别边 `skip(legality)` 属预期、下轮自愈。
- 自动模式：`BuildableBuilt/Placed` 防抖 3s + `autoIntervalSec` 心跳；**不订阅 `EnergyGridsUpdated`（自触发风暴）**；重入保护=`_session != null && !Done` 拒绝新 run；门禁 `IsPlaying && !IsMapOpen && !IsPaused`（Blocked 时手动才 Toast「当前无法连网…」，快照/Plan 失败路径也置 Blocked 防误报「电网已完整」）；OnGameEnd/OnDisable 丢弃会话。
- 日志策略（两 mod 同款，设计文档 §5）：恒记=生命周期 + 每次有变化的 `run:` 单行汇总（Toast 同文案 5s 去重，日志跟随 Toast 门）；verbose=快照统计/逐边 `link/drop/merge/skip(legality)`；连接器 >500 Warn「未做空间分桶」；无每帧输出。

### 3.8 flotsam.autopriority「自动任务优先级」v1.1.0
- 按键：**`Ctrl+3`** 开关自动任务优先级、**`Ctrl+4`** 开关自动加点（同 Ctrl+1/2 组合键约定：注册 `Alpha3/Alpha4`，`OnTick` 里 `IsDown && GameKeys.GetCtrlHeld()`）。HUD 两钮（右侧堆叠，可拖动记忆 `button.*`/`spendbutton.*`）：「优先级:自动/手动」+「加点:自动/手动」；**图标延迟到进存档首 tick 且 `NativeSkin.Available` 才 SetIcon**（同 batchmanager 的坑与修法；优先级钮=`HUD_DrifterExpertise` 图标，加点钮=❤ `UI_Icon_Affinity_Heart_28x28`，`Find` 均带退化 needles）。无窗口，反馈全走 Toast+日志+原生 UI（优先级格子/属性面板本身就是可视化）。
- 配置：`active`(**true**)、`autoSpend`(**true**)、`debounceSec`(0.5)、`tierHighBelow`(1/3)、`tierMidBelow`(2/3)、`verbose`、`button.*`、`spendbutton.*`、`schema`(1)。**两开关默认开**（用户点名要的自动化）；关闭只停手不回滚。
- 机制（**纯 public API，零反射零 Harmony**；全部封装在 Game 层 `GameAssignments.cs`，mod 层只管开关/事件/防抖/日志）：
  - **自动任务优先级 `RunAutoPriority`**：把全殖民地按任务类型分组（仅收「有对应技能」的任务：`Attributes.ReturnAssignmentAttribute(asn) != None`，天然跳过 Studying/Idle 等），组内按 `ReturnAssignmentAttributePoints`（游戏 `Assignment.ReturnPriority` 同款度量）做**相对排名**——对「去重后的技能值序列」按 `tierHighBelow/tierMidBelow` 切三段映射 高/中/次（**同值同档**；对 1 个高分+一堆 0 分的偏态分布也正确，因为按去重值组归一化而不是按人头）。全员同分跳过（无信号不动）；**永不设 None**（不会出现无人可做的任务）；单人不比（`cells.Count<2` 跳过）。写回 `Agent.TryUpdateAssignmentPriority`（原生优先级格同款调用），**写前比对 `Assignment.Priority` 不同才写** → 稳态零写入零游戏事件。
  - **自动加点 `RunAutoSpend`**：每人的 `SpendablePoints` 用原生 `DrifterAttributes.TryLevelAttribute` 逐点分配（= 面板「+」按钮同一调用，decompile 50725，事件/存档/通知链与手点完全一致）；选属性顺序=**❤爱好优先**（`ReturnAffinityAmount` 降序，心多者先）→**其次已最高属性**（专精：拉满强项再练下一项）；每人固定序遍历（加点只抬升已选项，等价于每点重排；满级自动落到次优，全满则点数保留不乱花，同原生）。**加点确有发生则强制跟一次优先级重排**（仅当 `active` 开）。
  - **事件驱动，无轮询**（用户明确要求去掉最初的 2s 心跳）：订阅 `AgentLevelGained`(→排队加点；攒点本身不改技能排名故不触发重排)、`AgentAttributeLeveled`(→重排；玩家手动加点同样覆盖)、`AgentAddedToPlayerCommunity`(→加点+重排，筛查新人携带的点数)、`AgentRemovedFromPlayerCommunity`/`AgentDeath`(→重排)。事件爆发经 `debounceSec` 防抖合并成一次，**统一推迟到 `OnTick` 执行**（不在事件回调里动游戏逻辑，避免 dispatch 中途重入）；读档 `OnGameStart` 延迟 1.5s 补跑一次；未进存档时手动开关→挂起队列+Toast「进入存档后生效」。**依赖 host v1.0.1 的事件层修复**（见第 1 节：读档清空全部订阅，host 在 IsPlaying 上升沿 RestoreAll）——v1.0.0 host 下本 mod 的事件全收不到，症状=「升级后不自动加点，必须手动切一次开关」（内联 Run 兜底所以切换有效）。`OnGameEnd` 重置 `_iconsSet`（同 batchmanager 图标坑）。
  - **无循环自激**：重排写优先级派发的是 `AgentAssignmentUpdated/AgentAssignmentInfo`（未订阅）；加点派发的 `AgentAttributeLeveled` 只再排一次重排队列，跑时无差异→零写入。`_running` 重入保护（重入转 Queue）+ 每次 Run 前 `IsPlaying` 门禁。
- 日志：OnLoad 配置一行；OnEnable ready 一行；手动开关 Toast 汇总（如「已为 3 名小人自动加点 7（❤爱好优先，其次高等级）；已按技能重排 5 名小人：调整 12 项（高4/中4/次4）」）+ `run(manual): spend[…] rank[…]`；事件触发**仅真有变化才一行** `run(event): …`（verbose 则空跑也记）；verbose 逐条 `priority 名字·任务 skill=N -> 档位`、`spend 名字: +N → 属性×n`。排错先找 `run(...)` 行。

### 3.9 flotsam.perfboost「性能优化与探针」v2.0.1
### 3.9 flotsam.perfboost「性能优化与探针」v2.1.1
- **v2.1.1（同日十九轮）**：v2.1.0 实测判读：**日志正常（零错误）但 Fixed 空转**——`fixed-rate on 179` 已应用，`DirectorFixedUpdate` 子项出数（0.1~0.7ms，Fixed 求值确实挪进了物理步）但 **DirectorUpdate 均值 17.9ms 纹丝不动**。铁证推理：**动画求值大头不在注册的 177 个 AlwaysAnimate 动画器里**——v2.0.1 冻结 179 个时它也没降。真凶锁定在 **29 个"skipped non-AlwaysAnimate"（游戏自选 culling 模式的动画器）或无 Renderer 被跳过的**。修法：注册条件放开——**全部动画器入册**（无论 culling mode；仅无 Renderer 的跳过——Freeze 需要它做可见性探针；Fixed 不需要），原 culling mode 记账照旧，Fixed/Freeze 对全员生效。若下轮 DirectorUpdate 终于下降=真凶在册；若仍不动 → 动画求值不走 Animator 组件路径（纯 PlayableGraph/Director 驱动，需再挖）。fps 30 行 29~37 与上轮持平；SRL 4.4~5.7、OD 4.7~9.6 稳定。
- **v2.1.0（同日十八轮）——靶单到手，半频求值上线**：v2.0.3 实测归属行出炉：**`animators: registered 177 (skipped non-AlwaysAnimate: 29); top owners: Morale_Decoration_×46 FarmingCrops_Potat×30 FarmingCrops_Tomat×25 BasicParent×20 FarmingCrops_Corn(×20 Main×13`**——**90% 是装饰物与农田作物的风吹摆动循环动画**（非机械/角色动画）。这对 `updateMode=Fixed` 是完美场景：**新增第三档 `animatorFixedUpdate`(1)**——全部在册动画器切 `Animator.updateMode=Fixed`（图求值从每帧挪进 50Hz FixedUpdate；摆动循环在俯视角下半采样率视觉无差）；`AnimatorState` 加 `FixedApplied`，`ApplyAnimatorFixed` 幂等应用（补扫时对新建筑的新动画器也生效）、`RestoreAnimators` 撤销回 Normal；日志 `offscreen animator culling on N (freeze tier armed) (fixed-rate on F)`；面板 `动画 N(半频F)(冻结M)`；OnGameEnd `SetAnimatorFixed(false)`。预期：DirectorUpdate 17~19ms 应减半（FixedUpdate 步进 50Hz 且物理相位已有余量：帧:FixedUpdate 现 0.5~0.7ms + ScriptRunDelayedFixedFrameRate 1.3ms，吃进动画求值后预计 2~3ms，净省 ~7ms/帧）。风险边界：若个别动画带行走根位移/精确对拍（装饰摆动无），Fixed 会造成 20ms 量化抖动——观察期发现异常改 `animatorFixedUpdate:0`。
- **v2.0.3（同日十七轮）**：修 v2.0.2 引入的补丁目标错误刷屏——`BlenableMaterial/MaterialBlender/SkyBoxBlender/VolumeBlender/BlenableCubeMap/NightLight` **经反编译逐类核实全部没有 Update/LateUpdate**（Blendable<>/Blender<> 基类也没有；NightLight 只有 Tween/SetColor）——v1.9.0 凭类名区间的臆测写错了 12 个目标，`RegisterHot` 每个失败打一条 Error。修正后材质混合家族只留真实存在的 `CircadianRotation.Update`；夜灯秒表删除；**教训：给 RegisterHot 加目标必须先在反编译里核实方法签名存在**（错误行 `[flotsam.perfboost] hotspot target not found: 材质混合(每实例)` ×12 曾刷屏）。
- **v2.0.2（同日十六轮）**：v2.0.1 实测：**冻结机制工作正常但收益封顶**——`animator freeze: 179 offscreen`（开地图瞬间全部冻结）与回屏后 `1/3/5/15/17 offscreen`；fps 29~35、1%低 12~22（与冻结前持平）。**结论：DirectorUpdate 18~20ms 是屏幕内动画的本体**——isVisible 路线已到顶（玩家大部分镜头城镇主体在屏内，相机 frustum 覆盖屏幕外物体）。**本轮加归属诊断**：扫描时打 `animators: registered N (skipped non-AlwaysAnimate: M); top owners: 名字×数 …`——搞清 179 个动画器挂谁身上（建筑 mesh？小人 rig？装饰？）再选下一杠杆（`Animator.updateMode=Fixed` 把图求值挪 50Hz FixedUpdate / `speed` 全局降档 / 动画 LOD）。同时观察：`ObjectDispatcherPostLateUpdate 5.5~9.7ms` 稳定第三大项（引擎对象派发相位）、`FinishFrameRendering 3.5~7.4ms` 随 DC 波动。
- **v2.0.1（同日十五轮）**：v2.0.0 实测（短会话 5 快照）：**冻结未生效**——`帧:Update/DirectorUpdate` 仍 19~20ms；在册 179 动画器但日志无 frozen 计数（verbose 门）且 fps 31~33 与上轮持平。**头号假设：`Renderer.isVisible` 判定失效**（俯视角相机 frustum 大于屏幕范围 / 阴影投射保持 renderer "visible"）。本轮改法：冻结刷新接日志回调（每次变化必打 `animator freeze: N offscreen`）+ **探针诊断行**（`animator freeze probe: V/179 renderers report visible`——若 V=179 即判定失效实锤，下轮换距离级判定：距主相机水平距离 > 视距阈值即冻结）。顺带发现日志里 ObjectDispatcherPostLateUpdate 5.5~6.9ms 稳定在列（引擎对象派发相位，与 ScriptRunBehaviourLateUpdate 5.8ms 同为下一梯队靶子——后者游戏侧 LateUpdate 未插桩候选已列：SelectionStateManager(每帧 IsInteractable)/BirdHouse/Hookable/AquaFarm/Desalinator/Radio/WalkwaySegment/DecorationSlots/VisualBoundary/JoystickCursor/DrifterOverview/CommunityMorale/FocusManager/Crosshair/DistanceCounter/SelectionFrame 等）。
- **v2.0.0（同日十四轮）——17ms 归属终审 + 冻结级优化上线**：v1.9.0 实测（29 快照）：二级子系统全面出数，**`帧:Update/DirectorUpdate = 19.6ms` 确认为主线程最大单项**——`Animator == PlayableGraph`（`Animator.playableGraph` 属性实锤），其图求值走 DirectorUpdate 相位，与脚本无关（我们已插桩的全部 Update 热点合计 ~1ms）；材质混合家族 0.1ms（洗清）、夜灯/电缆秒表未出数（未触发）。其他二级大项：`ScriptRunBehaviourLateUpdate 5.8ms`（游戏 LateUpdate 总量，其中已插桩 ~1ms——**LateUpdate 侧还有 ~4.8ms 未插桩对象**）、`ObjectDispatcherPostLateUpdate 5.7ms`、`FinishFrameRendering 3.4ms`、`UpdateAllRenderers 1.4ms`。**优化：`animatorFreeze`(1)**——离屏动画冻结级：`CullUpdateTransforms` 只停写变换但图求值照跑（19.6ms 的来源），冻结级按 `Renderer.isVisible`（引擎裁剪免费维护）每 0.5s 驱动 `Animator.enabled`——离屏 Animator **完全停机**（图求值也停），回屏即恢复；`AnimatorState` 加 `Renderer/Frozen` 字段，`RefreshAnimatorFreeze`（0.5s cadence，CullUpdateTransforms 在刷新间隔内兜底回屏正确性），面板/摘要显示 `动画 N(冻结 M)`，verbose 打冻结数变化。风险边界：动画事件（如风扇停转时的事件）在离屏期间不派发——游戏观察期正常（事件多为循环装饰动画），异常可 `animatorFreeze:0` 回退到纯 cull 模式。
- **v1.9.0（同日十三轮）**：v1.8.0 实测：**vsync 关闭生效**（`vSync -> 0`，monitorHz=170 原来等的是 144Hz 档；fps 31~34、1%低 10~24 较上轮 5~16 显著好转）；**`帧:Update = 18.4ms` 首次出数**（bug 修复生效）——相位拼图完整：Update 18.4 + PostLateUpdate 7.8 + PreLateUpdate 3.1 + Fixed 0.7 + 早族 0.2 ≈ 30ms ≈ 全帧。**但已插桩游戏脚本在 Update 相位里只占 ~1ms → 剩 ~17ms 在未插桩的 Update 调用**（MonoBehaviour.Update×大量实例 / coroutine / 引擎子系统）。本轮二级钻探：**(a) Update/PreLate/PostLate 相位的二级子系统逐个包裹计时**（level-2 PlayerLoop 重排，日志多行 `player loop phases:` 列出全部子系统名——ScriptRunBehaviourUpdate vs coroutine vs native 直接分流）；**(b) 材质混合家族聚合秒表**（decompile 36559~38800 区间全是 `Blenable*`/`SkyBoxBlender`/`VolumeBlender`/`Circadian*`——每实例 Update 的经典嫌疑，334 建筑×每材质一个：`材质混合(每实例)` 聚合 BlenableMaterial/MaterialBlender/SkyBoxBlender/VolumeBlender/BlenableCubeMap/CircadianRotation/CircadianVisuals 的 Update+LateUpdate）；**(c) `夜灯(每个)` NightLight 与 `电缆跟随(每根)` EnergyGridConnection.Update（39223 Lerp+scale 每帧每根）**。次要：HarmonyX 提示 URP Render override 应打基类声明方法——无害（计时已验证工作），暂留。
- **v1.8.0（同日十二轮）**：v1.7.1 实测帧解剖收齐（352 快照会话）：**相位均值=PostLateUpdate 8.0ms（渲染提交，含渲染管线 4.8/主相机 4.2）+ PreLateUpdate 4.4ms（动画/粒子/画布）+ FixedUpdate 1.4 + 脚本 1.5 + 早更新族 0.5 ≈ 15.8ms**；主线程计数 35~54ms 远大于相位和 → 差值 ≈ 6.9ms = **恰好一个 144Hz 刷新周期（vSync=1 在等呈现）**。尖峰归因：**GC 全量回收**（会话 53 次，堆 280→655MB，约 28s 一次；PLU 峰 20~153ms/PoLU 峰 20~144ms 的频率与 GC 频率吻合——停顿落在哪个相位就算哪个相位的峰）；另见单次 47ms 相机渲染尖峰（疑似 shader 编译）。**修复自己的测量 bug：`帧:Update` 全会话缺失**——`HotspotFrame`（自愈清栈，PerfOverlay.Update 每帧调用）在 Update 相位进行中把**相位开始时间戳当孤儿清掉了**（相位计时与热点计时共用 `_hotStarts` 栈）→ 修法=**相位栈 `_phaseStarts` 与热点栈分离**（各自 ThreadStatic，互不误伤；热点侧自愈逻辑不变）。**新优化项：`vSync`(默认 0=关，原版 1)/`targetFps`(默认 0=不限)**——吃掉呈现量化等待，本作慢速移动画面撕裂几乎不可见；`SetFramePacing/RestoreFramePacing`（QualitySettings.vSyncCount+Application.targetFrameRate，进程级，OnDisable 恢复）；pipeline 日志加 `monitorHz`。**会话级劣化全景**：fps 34→21 随 Mono 堆 280→655MB 增长（GC 越来越慢）+ 建筑 295→334；GC 根治无运行时 API（`UnityEngine.GarbageCollector` 本 build 不存在，boot.config `gc-max-time-slice=3` 未见效果）——若要根治需定位分配大户（下下轮候选：给高频游戏方法挂 GC 分配探针/减少日志与字符串拼接）。
- **v1.7.1（同日十一轮）**：v1.7.0 首轮相位数据**发现结构性盲区**——日志只见 帧:TimeUpdate/EarlyUpdate/FixedUpdate/PreUpdate（全属 EarlyUpdate 家族），**Update/PreLateUpdate/PostLateUpdate（脚本/动画/渲染提交三大主相位）完全缺失**：v1.7 的实现只给「有 subSystemList 的相位」插计时器，而这三个主相位在本 build 里 subSystemList=null 被静默跳过——**剩余 15~25ms 正藏在这里**。修法=**顶层包裹式**：根 subSystemList 重排为 [begin(P0),P0,end(P0),begin(P1),P1,end(P1),…]（UniTask PlayerLoopHelper 同款手法，与相位内部结构无关），安装时打一行 `player loop phases:` 日志列出全部相位名与是否有 subs。同轮判读 v1.7.0 数据：UGUI 40ms 仅加载帧（平时 0.1~0.4ms 洗清）；**GC 疑云**：g0=g1=g2 恒相等且 124→145——Boehm GC 无分代、每次都是全量回收，堆 400~560MB 时全量=10~40ms 停顿，与 UGUI/画布峰 10~40ms 及 1%低(5~16) 吻合；`gc-max-time-slice=3` 在 boot.config（增量 GC 运行时 API `UnityEngine.GarbageCollector` 本 build **不存在**，反射全 Managed 扫过；若下轮相位数据证明 GC 是大头，再评估 boot.config 实验）。Mono 堆 268→442MB 持续增长。帧均值结构：主线程 25~46ms，已归因≈ 渲染管线2~7.4+FixedUpdate 1~1.8+脚本1.5+UGUI 0.2 ≈5~10ms。
- **v1.7.0（同日十轮）**：v1.6.0 探针全面出数（Start() 修好），一整个会话（326 快照）的**完整帧解剖出炉**：主线程 21~35ms 是唯一瓶颈；可计量部分=主相机栈 4.3ms+提交 2.4ms+脚本 1.5ms+探针相机 0.4ms ≈ 9ms，**~17ms 仍未归因**（引擎原生相位）。会话级劣化实锤：**Mono 堆 284→562MB 翻倍**、阴影投射 60→~1400、DrawCalls 2978→4403、fps 37~45→30~32（+19 建筑）；GPU帧 0→5.6ms（GPU 有活了但仍闲）、渲染线程 2.7~3.7ms、RT 40 张/225MB 稳定（无泄漏）、开世界地图时 DC 543/RT 18（地图态便宜）。markers 终局认知：**release player 里时间型 marker 全被裁**（主相机/阴影/裁剪/GC.Collect 等 taps 恒 0 属编译期决定，已从面板删除）；**能出数的全是计数器**（主线程/渲染线程/GPU帧/GC内存/DrawCalls/SetPass/三角形/批次/阴影投射/RT）。本轮补齐最后拼图：**PlayerLoop 相位计时**（Unity 6 API 是 `GetCurrentPlayerLoop()` 返回**单个根 PlayerLoopSystem**（非数组！），每个顶级相位 subSystemList 首尾插计时委托、保存原始根 OnDisable 恢复；相位=帧:Update/帧:FixedUpdate/帧:PreLateUpdate(动画)/帧:PostLateUpdate(渲染提交)/帧:EarlyUpdate/帧:Initialization/帧:TimeUpdate）+ **渲染管线总量**（URP 两个 `Render` override 前后缀，深度守卫防 List→数组转发的双计）+ **UI画布渲染**（`Canvas.SendWillRenderCanvases`，private static）+ **GC 回收计数**（`GC.CollectionCount(0/1/2)`，快照行 `GC回收 g0/g1/g2`——验证 1% 低帧尖峰是否 GC 暂停）。下轮数据将把 26ms 完整归因到相位 → 对症下药（物理步？动画？渲染提交相位？）。
- **v1.6.0（同日九轮）**：v1.5.0 实测三连破案：**(a) 探针全 0 的根因终于找到**——ctor 矩阵显示所有构造 `IsRunning=false`，**`ProfilerRecorder` 在本 player 构造后不会自动启动，必须显式调用 `Start()`**（反射确认该 public 方法存在；此前五轮的 handle/(Category,Name)/nameOnly/3 构造自愈/无枚举实验全是在排查错误的假设）——现在 tap 创建后立即 `Start()`，`forceProfiler` 理论也被否定（profiler enabled 也没让未 start 的 recorder 出数），**保留 forceProfiler 但其真实作用存疑**；**(b) 主相机渲染路径找到**——只见到 Reflection Probes 相机的原因：base 相机（Main/UI/FOW）走 **`RenderCameraStack(ctx, baseCamera, bool)`**（私静态，反射确认参数名 `baseCamera`），非 `RenderSingleCameraInternal`；新增 `StackRenderPre(Camera baseCamera)` 前缀（Harmony 按参数名绑定，与 `CameraRenderPre(Camera camera)` 区分），补 `RenderCameraStack`+`RenderSingleCameraInternal`×2 共 3 个入口，同名嵌套去重防双计；**(c) 发现会话内劣化**——**渲染提交（Submit）0.8ms→3.0ms 随时间增长，fps 44→34 同步下滑**；UGUI 重建仅 0.1~0.2ms（洗清）、Reflection Probes 0.4ms、脚本合计仍 ~1.5ms。为追劣化加**内存观测**：`GamePerf.TotalAllocated/Reserved/MonoUsedBytes`（`Profiler.GetTotal*MemoryLong`+`GC.GetTotalMemory`，不依赖 recorder）+ 新 taps `Render Textures Count/Bytes`（渲染纹理泄漏=FOW 相机头号嫌疑）+ fps 行与面板均带 `堆内存/预留/Mono` 内存行。v1.5.0 其他收获：`additionalLightShadows=False`（无逐灯阴影乘法，排除）、`vSync=1`（帧时间未按 16.6ms 量化，非瓶颈）、`shadowmap 4096->2048` 已生效、profiler forced on 时帧率与上轮相当。
- **v1.5.0（同日八轮）**：v1.4.0 实验（tap 先建+无枚举）仍全 0 →「枚举毒化」理论也否定；帧率 37~40（级联 4→2 无可见增益）。换打法：**不再猜 ProfilerRecorder，直接秒表掐渲染管线托管入口**（枚举清单给出确切方法名，全是可补丁的 C#）：① **逐相机渲染计时**——prefix/postfix 挂 `UniversalRenderPipeline.RenderSingleCameraInternal` 全部两个重载（`(ctx,camera,bool)` 与 `(ctx,camera,ref data,bool)`，反射确认），`GamePerf.CameraRenderBegin/End` ThreadStatic 双栈+同相机嵌套去重（3-arg 调 4-arg 场景），热点行「相机:Main Camera」等——游戏有 Main/UI/FOW/StaticPortrait/ReflectionProbes 五台相机在跑，哪台吃帧一目了然；② **UGUI 重建** `CanvasUpdateRegistry.PerformUpdate`（public static，UnityEngine.UI.dll 已在 Directory.Build.props 全局引用）——canvas 密集型游戏的经典隐藏大头；③ **渲染提交** `ScriptableRenderContext.Submit`。**探针死数据三连实验**：每个快照打 `ctor matrix:` 行——同一 marker（DrawCalls/GC内存）× 5 种构造（nameOnly cap1/cap15/opts=None、`ProfilerCategory.Render+name`、2-string、`new ProfilerMarker(name)`）各报 `LastValue/Count/IsRunning`（Count>0=数据流真伪铁证）；`forceProfiler`(1) 开关——**`Profiler.enabled` 强制开启**（release player 里计数器可能只在 profiler 取帧时流动的头号嫌疑；GamePerf.EnsureProfilerStats/Restore，日志会打 `profiler engine was OFF — forced on`）。**新优化项**：`shadowmapRes`(2048 默认，原版 **4096**)——阴影图分辨率，配合级联 2 把阴影 pass 开销打到原版的 ~1/8；pipeline 日志扩为 `srpBatcher/cascades/mainShadowmap/additionalLightShadows/vSync/targetFps`（后三项新观测，additionalLightShadows 若原版开着=阴影 DrawCall 再乘每盏灯，是下一个靶子）。mod csproj 新增 URP 引用（相机补丁用）。
- **v1.4.0（同日七轮）**：v1.3.0 实测收获：**`pipeline: srpBatcher=True, shadowCascades=4, mainShadowmap=4096`**——SRP Batcher 原版已开（无增益可吃）；**原版 4 级联 × 4K 阴影图**才是新靶点：级联=把阴影距离分段分别渲染，每段都要**把全部阴影投射体重画一遍**（266 建筑 × 4 段 = 每帧 1000+ 次阴影绘制）；我们的距离已钳到 100，2 级联足够覆盖且近处分辨率毫无损失 → `shadowCascades` **默认 0→2**（1=最大省、4=回原版、0=不动），日志 `shadow cascades 4 -> 2`。同轮修探针死数据：3 种构造全 0 后复盘全部会话——**唯一读到真数据的 18:09 会话（v1.0.0，DrawCalls 5186）是唯一没调用 `GetAvailable` 的会话**；v1.1.0 起先枚举再建 tap 的所有会话（含 3 构造自愈）全灭。决定性实验：tap **先建**（name-only 构造、枚举挪到 verbose 门控内、单 recorder/tap、单位改 Wanted 表硬编码），若下轮出数即坐实「枚举毒化数据管道」。另修 `CircadianMaterial` 只有 LateUpdate 无 Update 的 target-not-found 错误。UnityStats 反射查证：本 Unity 6000.3.16 build **没有** `UnityEngine.UnityStats`（DrawCalls 无该后路）。
- **v1.3.0（同日六轮）**：v1.2.1 实测定案两件事：(a) **脚本侧合计 ~1.5ms（28 个秒表方法），帧 24ms**——建筑×266 全部 Update 仅 0.1ms，小人/生产/寻路执行全部 0.1~0.2ms 级，全部嫌疑洗清；**~22ms 在引擎原生环节（渲染提交/裁剪/UI 重建）**，恰好是全 0 的 taps 本该回答的。(b) `(Category,Name)` 构造**同样** Valid 但零样本（18/18 live 全 0，连 GC 内存都 0）→ v1.0.0 能读到 5186 的构造方式未知。本轮三招：**① 探针多构造自愈**——每个 tap 同时挂 3 种构造（name-only/(Category,Name)/handle），连续 3 次读 0 就切到有数据的变体（一行日志 `probe tap 'X' healed via ctor #N`），前 3 次快照另打 `ctor variants: [...]` 诊断行标出每种构造读到的值；**② SRP Batcher 开关**（`srpBatcher` 默认开）——URP asset `useSRPBatcher` public settable 且游戏代码从不碰它；日志会打 `pipeline: srpBatcher=…, shadowCascades=…, mainShadowmap=…`（原版值可见），关闭态会被强制打开（CPU 渲染提交折扣，不兼容 shader 自动回落=安全）；**③ 阴影级联**（`shadowCascades` 默认 0=不动，可设 1/2/4）——级联数直接乘阴影 pass 绘制数。两者与阴影钳制同用 5s 延迟重试+OnDisable 恢复原值。探针自愈若生效，下一轮就有主线程/渲染线程/主相机/裁剪/DrawCalls 的真实数字 → 精确定位 22ms。
- **v1.2.1（同日五轮）**：v1.2.0 首轮实测（D3D11）两发现：(a) **17/17 探针 live 但全部恒 0**（连 GC Used Memory 都 0）——v1.1.0 起的 handle 构造 `new ProfilerRecorder(h,1,…)` 在本 player 返回 Valid 却**永远收不到样本**；对比 18:09 的 v1.0 会话（`(Category,Name)` 构造）当时 Draw Calls 能读到 5186 → 修法=改用文档写明的 `new ProfilerRecorder(desc.Category, desc.Name, 1, Default)`（`ProfilerRecorderDescription.Category/Name` 经反射确认存在），handle 仅作 fallback。**推论：19:03 D3D12 会话统计全 0 可能是同一 bug 而非 D3D12 黑洞——「D3D12 不喂统计」存疑待重测**（fps 无收益的结论不受影响）。(b) **14 个管理器热点合计 <1ms，帧耗时 26ms** → 大头在管理器之下的每实例 Behaviour Update（引擎直调，管理器秒表看不见）。新增 **5 个每类聚合秒表**：`Buildable.Update/LateUpdate/FixedUpdate`（全 266 座求和，decompile 16165/16170/16222）、`Producer.Update/LateUpdate`（24877/24902）、`Agent.LateUpdate`（5479）、`Navigator.Update/LateUpdate`（114825/114843，寻路执行）、`ParticleController.Update`（45185）；新增 tap `裁剪=ScriptableRenderer.SetupCullingParameters`；面板热点行过滤零值行（avg<0.05ms 且峰<1ms 不显示，但计入合计行）。本轮实测小数据：寻路图 0.3~0.6ms(峰5)、小人AI 峰 3ms、其余管理器 0.1ms 级。
- **v1.2.0（同日四轮）**：修「面板分项恒 `-` / 0」。用户 D3D12 实测反馈=帧率不变+数据全空。(a) `-` 根因=v1.0/v1.1.0 的候选名（BehaviourUpdate/Physics.Processing/Canvas.WillRenderCanvases 等编辑器内建 marker）**在 release player 里不存在**——v1.1.0 枚举的 217 个真实 recorder 证明可用的是：`CPU Main/Render Thread Frame Time`、`GPU Frame Time`、`Gfx.WaitForRenderThread`、`GC.Collect`、`GC Used Memory`、`Batches/Draw Calls/SetPass/Triangles/Shadow Casters Count`、**整套 URP marker**（`RenderSingleCameraInternal: Main/UI/FOW/StaticPortrait/ReflectionProbes Camera`、`Draw Main/Additional Lights Shadowmap`、`On Record Render Graph` 等）。Wanted 表全部重映射真名。(b) **脚本分项改自建秒表**：Harmony prefix/postfix 挂 14 个管理器入口（GameManager.LateUpdate 主泵/AgentManager.LateUpdate 小人AI/GraphManager Update+LateUpdate 寻路图/UIManager/WorldManager/WorldMap/EnergyGridManager/Engine/TooltipPanel/PhysicsManager.FixedUpdate/FlotsamInputManager/CameraZoomController/CircadianMaterial/AnalyticsManager）——泛型标记类型 `Hot<T>` 每 T 独立静态 Index（零委托分配）、`GamePerf.HotspotBegin/End` ThreadStatic 时间戳栈防嵌套、`HotspotFrame` 帧边界自愈清栈（目标方法抛异常漏 Pop 时防串号）、每帧均值+单次峰值（峰值抓 GC/爆发尖峰，正是 1% 低帧的来源）；配置 `hotspots`(1)；schema 1→2 顺带重置面板窗口尺寸（行数×3）。面板新增「— 脚本热点(均值/单次峰值 ms) —」区+脚本合计行；fps 日志行追加 `| 脚本 <热点行>`，快照后 `HotspotReset` 开新统计窗。(c) **D3D12 实验结论：生效但无收益**——Player.log 确认 `Direct3D 12 [level 12.2]`，fps 32.5~44（D3D11 基线 34~42.5）。
- **v1.1.1（同日三轮）**：修「阴影优化从未生效」——旧实现在进存档瞬间 `QualitySettings.renderPipeline` 尚为 null 就直接放弃（日志实证：`shadow: active pipeline asset is not URP`，`ShadowManager` 回退也够不着）。新机制=**上限钳制**：prefix 挂 `ShadowManager.SetShadowDistance`（游戏 `CameraZoom` 按缩放曲线调它，decompile 35391——旧的一次性静态写入即使成功也会被下一次缩放覆盖，属设计层缺陷），经 `GamePerf.ClampShadowRequest` 钳制；`ApplyShadowCap` 三路由找 URP asset（`QualitySettings` → `GraphicsSettings.currentRenderPipeline` → 反射 `ShadowManager._instance._renderPipeline`——进存档最初几秒三路都可能为 null，OnTick 每 5s 重试直至生效，日志 `shadow cap 100 applied (now …)`）；GameEnd `ClearShadowCap()`（主菜单回原版）、OnDisable `ResetShadowDistance`+回写记录原值（防 `_cacheDefaultShadowDistance` 缓存被污染）。同轮顺手修 **autocrew 刷屏**：`GameCrew.cs` 逐条 landmark 的 `landmark boat=…` 日志漏 verbose 门控（心跳期 2 行/秒的根因），且 "was" 值在写入后才读恒等于新值——先存 `before` 再写。另：游戏根目录新增 **`启动-D3D12.bat`**（`-force-d3d12`，游戏自带 `D3D12\D3D12Core.dll`）=把 ~5000 DrawCall 的提交压力移出 CPU 主线程的实验（v1.1.0 主攻方向的另一半）；回滚=正常启动即可，Player.log 出现 `Direct3D 12` 即生效。首轮实测基线（D3D11、perfboost v1.0 部分生效）：**35~42 fps、1%低 13、Draw Calls 3800~5200、SetPass ~570、等GPU 0ms**（266 建筑/18 小人）。
- **v1.1.0（同日二轮）**：探针改为 **`ProfilerRecorderHandle.GetAvailable` 运行时枚举 + 按名匹配**（Unity 6000.3 改了一批 marker 名，v1.0 按旧名猜的全无效；枚举清单会完整打进日志供后续调优）；新增 `lodBias` 开关。首轮实测数据：「等GPU」恒 0（CPU 瓶颈实锤）、**Draw Calls ≈5000 / SetPass ≈478**（266 建筑/18 小人）→ 当前主攻方向=减 DrawCall（阴影 pass 是其最大倍增器；`shadowDistance=0` 可整轮砍掉阴影 pass）。注：URP 的 `supportsMainLightShadows` 等 setter 是 internal（元数据可见但编译报只读），别走反射硬改——`shadowDistance=0` 等效且公开。
- **v1.0.0**：初版。
- 背景：用户报"没用显卡、建筑多了卡"。诊断结论=**渲染确实在 RTX 4070 SUPER 上（Player.log 启动段），瓶颈在 CPU 主线程**；任务管理器 GPU 0% 是显卡在等 CPU。设计文档：`specs/2026-09-21-perfboost-design.md`。
- 按键：**F9** 开关性能面板（另有 HUD 钮「性能面板」，LeftTop）。配置：`showOverlay`(1)、`disableCtaa`(1)、`suppressLogSpam`(1)、`shadowDistance`(100，0=不动)、`shadowCascades`(2，0=不动/4=原版)、`shadowmapRes`(2048，0=不动)、`srpBatcher`(1)、`forceProfiler`(1)、`cullOffscreenAnimators`(1)、`particleRateScale`(0.5，1=不动)、`renderScale`(1=不动)、`fixedDeltaMs`(0=不动，实验)、`lodBias`(1=不动)、`hotspots`(1，脚本+渲染秒表补丁组)、`logIntervalSec`(10)、`verbose`、`window.*`、`button.*`、`schema`(2)。
- 机制（全部游戏侧逻辑在 Game 层新文件 **`GamePerf.cs`**，逐实例记账、OnDisable 全量回滚）：
  - **CTAA 空转**：游戏是 URP（`CameraOverlays` 用 `UniversalAdditionalCameraData`、RenderGraph using），`CTAA_PC.OnRenderImage` 永不执行，但其 OnEnable 仍给 3 台相机强加 `Depth|MotionVectors`、OnPreCull 每帧投影抖动。mod 置 `CTAA_Enabled=false`+`enabled=false`，**只清 MotionVectors 标志**（Depth 保留——`EnableDepthInForwardCamera`/`EnableCameraDepthInForward` 两个组件证明水面依赖深度图）；恢复=重新 enable（其 OnEnable 自己加回标志，精确回原版）。OnTick 每 5s 幂等重扫，兜底晚激活的相机。
  - **日志降噪**：prefix 跳过 `ParticleController.OnDestroy` 与 `PajamaLlama.PrefabPool.PrefabReference.OnDestroy`（两方法体经核实**仅含**一条日志调用）；`Application.SetStackTraceLogType(Log/Warning, None)`（Error 保留栈）。
  - **阴影/渲染比例**：阴影=v1.1.1 的上限钳制（见顶部 v1.1.1 条目）；渲染比例=写活跃 URP asset `renderScale`。注意 Game csproj 因此新增 `Unity.RenderPipelines.Universal.Runtime.dll` 引用（Private=false）。
  - **离屏动画剔除**：GameStart 全扫 + BuildableBuilt/Placed/AgentAdded 事件置脏后 3s 节流补扫；只动 `AlwaysAnimate` 且子树含 `Renderer` 的 Animator（跳过游戏自选剔除模式与 UGUI 动画）。
  - **粒子瘦身**：postfix `ParticleController.Initialize(Transform,Vector3)`（池化 Spawn 两条路径都经它，45208），按实例 ID 只缩一次 `emission.rateOverTime/ rateOverDistance Multiplier` 与 `main.maxParticles`；`_particleSystem` 私有字段经缓存 FieldInfo 读取。
  - **探针 `PerfProbe`**：`ProfilerRecorderHandle.GetAvailable` 枚举全部 recorder 按名精确匹配（v1.2.0 起映射真名，见顶部 v1.2.0 条目；D3D12 下渲染统计恒 0 属后端盲区）。**Unity 6000.3 里 `ProfilerRecorder/ProfilerCategory` 在 `Unity.Profiling` 命名空间**（不再是 `UnityEngine.Profiling`，CS0246 踩过）。FPS=ring buffer(240) 均值 + 1% low；每 `logIntervalSec` 秒写一行 `fps … | 主线程 … | 渲染线程 … | 脚本 <热点行> | 建筑 N 小人 M`。脚本分项=**自建秒表**（`GamePerf` 热点节 + mod 层 `Hot<T>` 补丁组，14 个管理器入口，均值+峰值）。
- 面板：`PerfOverlay`（SailHud 同款懒构建：IsPlaying 才建、GameEnd 销毁重建；UiWindow ContentMode.None；4Hz 刷新）。
- 验证：v1.0~v1.1.1 均已进档实测（v1.1.1 阴影钳制已确认生效：`shadow cap 100 applied (now 100)`；D3D12 实验完成，结论见 v1.2.0 条目）；**v1.2.0（探针重映射+脚本热点）已装待实测**——进档后看 F9 面板「主线程/渲染线程/脚本热点」是否出数、fps 日志行是否含 `| 脚本 …`，数据即下一轮优化的靶子。

---

## 4. 逆向结论速查（decompile 行号）

- `WorldMap.Deactivate` 159716：结尾 `AddMovementBlocker(this)`；`Activate` 159695 移除。
- `MovementEvent.DispatchTownheartMove` 40865 → `World.OnTownheartMove` 156676 → `RepositionTownheart` 156616 → 每瓦片 157766（ClearSpawners + 重 Spawn + `UpdateAllObstacles`）。
- `WorldMapTownheartPhycis2D` 163815：`MoveWithKeys` 164000；`ApplyMovementAndRotation` 164058（movement==0 直接 return → 不能原地转）；`Rotation` 163889 = `Euler(0,-rb.rotation,0)`；2D up = 世界 +Z。
- `BuildableTooltip`（建造悬浮框）约 81340-81500：`HideTooltip()` 直接 `gameObject.SetActive(false)`；槽为 `BuildableTooltipItemSlot : BuildableTooltipSlot, GUIItemSlot`（`Initialize(CountedItemProperty,bool)` 81529）。
- `TooltipPanel` 82000：`Update` 每帧 `PositionTooltip`；`HideTooltip_Internal` 82195。
- `RecipeItemDisplay` 70891：`IPointerClickHandler` + **public** `OnLeftClickEvent/OnRightClickEvent`（无需反射即可挂监听）。
- `ProductionPanel` 69381；`Producer.Recipe` 24577（`Ingredients/ProducedItems/AmountToProduce/ToggleContinuous`）；`Producer.IncreaseSelectedRecipeAmountToProduce` 25210。
- 打捞成分：`LandmarkInteractableWithComposition.Composition` 102466。
- 素材名/字体：见 `Mods/nativeskin_names.txt` 与日志 `native skin:` 行。

### 电网与批量操作（powerlink / batchmanager 逆向）
- 电网核心：`EnergyGrid.Connect/Disconnect`（static）39166/39182（Connect=双向 Connect+`Merge`+派发 `EnergyGridConnectionAdded`，电缆可视由 `EnergyGridConnectionVisualizer` 39244 监听事件自动生成）；`EnergyGridManager.Grids`（static）39706；`EnergyGridConnector` 39370（`CanConnect()`=空槽 39660、`IsInRange` 用 `BuildableSettings.CableLinkRange` 39612（默认 20f，142354 附近）、`Connections[]`、`IsTownheart`）；建筑组件 `EnergyGridBuildableComponent` 20505（`CanConnect` 要求 `BuildPhase==Finished` 20717；`Finish()` 自建独立 grid 20568）；电线杆 `EnergyGridPole` 20885（杆=高容量连接器，玩家建造；**与连接器同 GameObject**——`Initialize` 20912 `GetComponent<EnergyGridConnector>()`，杆建筑同时挂 EnergyGridBuildableComponent，识别杆必须用 `GetComponent<EnergyGridPole>()` 而非组件类型判断）。
- 连线合法性完整出处：`EnergyGridConnectCursorProperties.Connect/UpdateComponentsInRange` 90325/90417（距离取 `ConnectionTransform.position.Leveled()`，`Leveled()`=y 归零 205100）；带电建筑动作入口 `BuildableActionConnect` 4076。
- 电网状态：`EnergyGrid.HasEnergy/GridEfficiency/ReturnEnergy{Production,Requirement,Consumption}/ReturnStorage{Energy,Capacity}/IsTownheartGrid` 38795-39126；缺电故障 `ErrorNotLinkedToEnergyGrid`（无连线自动挂/连上自动消）39566。
- 批量操作：`Buildable.Salvage/CancelDeconstruction/Upgrade/CancelUpgrade/CanUpgrade/CanBeDeconstructed(out LocalizedString)/Activate/Deactivate` 16718/16763/16589/16560/16643/16891/16436/16446（原生按钮动作 `BuildableActionDeconstruct/Upgrade/OnOff` 4156/4320/4246 调的就是这些）；面板开关标志 `Properties.ShowActivationElement` 17985、拆除标志 `ShowDurabilityElements` 17997、升级链 `Properties.Upgrade` 17904；世界高亮 `OutlineRendererComponent.UpdateSelectedObject/ResetHighlightOutline` 165096/165178（`HighlightManager.HighlightObject` 108516 同款）；`OnDeselected` 会清高亮 17244。
- `PopUpDialog` 57323：只有文本输入（`TryPopUpInput`）与预置 `DialogProperties` 对话，**无可自定义文案的通用 yes/no API**。

### 平衡系统（gameplaytweaks 逆向）
- **重量不影响最高速度**：`WorldMap.MovementSpeed => EnergyDevTools.ApplyMovementSpeed(_movementSpeed) * _engine.ReturnMovementSpeed()`（159656），`_movementSpeed` 是定值(50)，`Engine.ReturnMovementSpeed()=Buildable.ReturnModifier(ModifierType.MovementSpeed)`（22359）只吃**模块**修正，与重量无关。重量的真实代价是**每单位距离能耗**：`Engine.ReturnEelsPerUnit()=GameplaySettings.ReturnEelsPerUnit(TownWeight)*ReturnModifier(MovementEnergyCost)`（22405）→ `ComputeEelsPerUnit(weight)`（143089，Function 模式 `offset + w*scalar + base^(max(0,w-1))`，w=weight/1000；Tiers 模式按阈值查表）。能量耗尽→`IsCoolingDown`→`ReturnMoveableDistance=0`，所以"重→费电→频繁冷却→更慢"是间接的。
- **载重上限（能否再建）**：`PlaceableProperties.ReturnCanBePlaced`（18065）里 `flag3 = IgnoreWeightForPlacement || Engine.CanTug(this)`；`Engine.CanTug = 建筑重量 <= TownAvailableTugCapacity`（22441）；`TownAvailableTugCapacity=max(_townTugCapacity-TownWeight,0)`、`_townTugCapacity=ReturnTownTugCapacity()`（= 所有启用引擎 `_tugCapacity` 之和）在 `Engine.LateUpdate`（22255-22257）每帧算。**没有"分档硬禁建"**，玩家感知的"八级建不了"其实是 weight 触顶 capacity → available=0。`TownUsedTugCapacityPercentage` 驱动 `Engine.WeightTier`（ScriptableObject，22565，只含 EelsPerUnit/Color，供载重条 58301 与配色）。
- **发电**：`EnergyGrid.ReturnEnergyProduction()=Σ producer.Production`（39070），`CalculateEfficiency()`（38834）每帧 `production*dt` 入网。三类 `IEnergyGridProducer`：`EnergyPassiveGenerator.Production=EnergyRate`（风/水，21711）、`EnergyItemProducer.Production=PowerRate`（燃料，21098）、`EnergyManualProducer.Production=ReturnAgentEnergyGeneration()`（人力，21431）。改 `Production` getter 即改实际入网电量（且 `ReturnEnergyProductionExcludingProducer`/优先级判断同源，保持一致）。
- **美观度**：`Community.BeautyScore`（7344）= Σ `construction.Buildable.ReturnBeautyScore()`（`UpdateBeautyScore` 7783，缓存 int，变化时发 `BeautyScoreUpdated`）；`Buildable.ReturnBeautyScore()=Properties.BeautyScore + DecorationSlots.BeautyScore`（17387）。**影响士气**：`TownBeautyMoraleEffect`（命名空间 `PajamaLlama.Flotsam.Morale`）在 `UpdateModifier`（217339）按 `_modifierThresholds`（`Threshold<=BeautyScore` 取该档 `Modifier`）算出 `_modifier`，`ReturnModifier()`（217301）被 `Morale.ReturnMoraleModifierSum`（217798）累加。改建筑分要 `UpdateBeautyScore()` 重算缓存；改士气读取处 `ReturnModifier()` 即时生效。

### 小人等级与任务优先级（autopriority 逆向）
- 小人=`Agent`（5279，全局命名空间，无 namespace）；玩家名单=`Community.PlayerCommunity.Agents`（7310）；`AgentManager` 105703（新人经 `ApplyAssignmentTemplate` 5746 从 `AssignmentPriorityTemplates` 拿默认优先级；模板初值全 Lowest，105731）。
- **任务优先级只有 4 档**：`AssignmentPriority`（13887）=None/Lowest/Default/Highest；原生优先级格左右键就循环这 4 档（`AssignmentPriorityBoxBase.UpdatePriority/Refresh` 51953/51992）。**nativeskin 里的 `UI_Icon_InfoPanel_Priority_VeryHigh/VeryLow` 图标属别的系统，别被误导成 5 档**。任务类型 `AssignmentType`（13862，Flags）：建造/浮标打捞/液体处理/钓鱼/搬运/制作/烹饪/地标交互/畜牧/研究/电力管理/医疗/植物/工程/建筑/耕作/Studying/Idle（另有两个 Deprecated）。每人每类型一条 `Assignment`（13895，`Type/Priority/Enabled` 均 public）。
- 写入口 `Agent.TryUpdateAssignmentPriority(type, priority)`（5775）→ `SetAssignmentPriority`（5809：当前项目不再保优先则 `Stop(ProjectFlags.Priority)` 中断 + 置重排标志 + `GlobalHaulingPriorities.Update`）。**返回值恒 false（游戏自己的笔误），别拿它当成败**。选择权重 `Assignment.ReturnPriority()`（14009）= 档位×1000 + 爱好×100 + 相关技能分——档位主导、技能只做同档细排。
- 等级系统 `DrifterAttributes`（8882，每小人一个实例，`Agent.Attributes` 5350）：总等级 `Level`（上限=`MaximumAttributeLevel`×8，9107/9109）；14 种 `AttributeType`（8884：建造/运动/液体/回收/研究/烹饪/钓鱼/打捞/农耕/医疗/植物/工程/建筑）；单项等级=`Expertise`+效果点=`ReturnTotalAttributePoints`（9258）。任务↔属性映射是**数据驱动**（`Attribute.AffectedAssignments` 8939，ScriptableObject 资产里配）：`ReturnAssignmentAttribute(asn)`（9374，取首个影响该任务的属性，**无对应技能返回 None**——autopriority 用它筛"别管"的任务）、`ReturnAssignmentAttributePoints(asn)`（9410，求和全部相关属性）。
- 加点：`TryLevelAttribute(type)`（9214）= SpendablePoints-- + AddExpertise + 派发 `AgentAttributeLeveled`；面板「+」按钮调的就是它（50725 `DrifterExpertiseField.Apply`，一次点击循环调 N 次）。经验→升级：`AddLevel`（9191：Level++、SpendablePoints++、派 `AgentLevelGained` + `LevelIncreasedEvent`，并给社区加研究点——**自动加点不会漏研究点，它们挂在升级上而非花点上**）。
- ❤爱好：`ReturnAffinityAmount(type)`（9364）= Effects（`DrifterAttributesEffect`）里该属性 Affinity 之和；UI 心数就是它（50689/49189）。
- 事件（`GameEventType` 41663）：`AgentLevelGained`=2027（9210 派）、`AgentAttributeLeveled`=2030（9231 派）、`AgentAddedToPlayerCommunity`=205（7490 派）、`AgentRemovedFromPlayerCommunity`=206（7541 派）、`AgentDeath`=202（5655 派）。**`ActorEvent.DispatchEvent` 双派发**（39879：先按原始类型派一遍，再按映射类型 ActorDeath/ActorRescue 派一遍）→ 按原始名 `Events.On("AgentDeath",…)` 一定收得到。
- **事件总线大坑**：`GameEventDispatcher.RemoveAllGameEventListeners()`（41512）被 `LoadingScreen.LoadSceneCoroutine`（105273）在**每次场景加载开头**调用——一切 boot 期 `AddListener` 全灭（host v1.0.1 已改为 IsPlaying 边沿轮询 + 进存档 `RestoreAll` 重注册，见第 1 节）。另：无参 `GameEventDispatcher.Dispatch(GameEventType)`（41532）以 **null** 事件对象调 `UnityAction<GameEvent>` 监听器——handler 忽略参数即可安全接收（`AddLevel` 的 AgentLevelGained 就是这么派的）。

---

## 5. 接力开发指南

1. 先读：本文件 → `ModKit/设计与实现方案.md` → 目标 mod 源码 → 需要时查 `_mod_recon` 行号。
2. 改代码 → `ModKit\build.ps1` → **重启游戏**验证 → 读 `BepInEx\LogOutput.log`。
3. 排错顺序：日志里该 mod 的行为日志（pin/resolve/aim-fill/capture/settled）→ 补丁注册行 → 再怀疑逻辑。
4. UI 改动遵守第 2 节约定（原生皮肤、窗口外壳、提示回收、位置记忆），不要新造一套样式。
5. **不要用 PowerShell `Set-Content` 打补丁改 .cs**（曾整文件覆盖事故）；用编辑工具做精确替换。
6. 新增每帧逻辑：mod 类里用 `OnTick`，或建 driver GameObject；不要在 mod 类写 `Update()`（不会执行）。
7. 配置新增键记得：默认值 + 必要时 `schema` 迁移（旧 config 会残留）。
8. 性能红线：小地图/材料面板都是高频刷新；遍历生产者/瓦片要用快照 API；`FindObjectsOfType` 只允许低频或一次性调用。

## 6. 待办 / 未决项

- [ ] **perfboost 进存档实测**（安装需先关游戏——Host/Game DLL 被运行中的游戏锁定）：① 日志应见 `perfboost loaded` + `2~3 patch(es) applied` + `ctaa disabled on 3 component(s)` + `offscreen animator culling on N animator(s)`；② F9 面板出数、各分项毫秒非 `-`（无效 marker 会在 `perf overlay built — …` 行列为 missing）；③ 面板「等GPU」≈0 佐证 CPU 瓶颈；④ 玩 10 分钟后把日志里 `fps …` 快照行发回，据此定第二轮优化（若 `脚本 Update` 占大头 → 深挖具体 Update 热点；若 FixedUpdate/物理高 → 开 `fixedDeltaMs=33`；若「等GPU」高 → 开 `renderScale=0.85`）；⑤ F10 禁用 mod 后回滚无残留（CTAA/阴影/动画/粒子全恢复）。
- [ ] 用最新构建复验：钉住→点缺料→`resolve:` 日志出现且只定位不排产；按住 G 点建筑→`aim-fill:` 排产。
- [ ] 复验六个窗口"隐藏提示后无空白带"（Footer/摘要/控制区/滚动列表底边/BatchPanel 操作栏平移+列底边回收，共五套回收逻辑）。
- [ ] **batchmanager/powerlink 进存档实测**（加载已验证 0 报错，行为待验）：设计文档 `specs/2026-09-20-batchmanager-powerlink-design.md` §7 清单 1-11（勾选高亮/逐个跳转/批量拆除确认条/升级跳过汇总/Ctrl+2 连网/整理重构/互济合并/自动模式/存档保留/日志卫生）。
- [ ] **autopriority 进存档实测（含 host v1.0.1 事件层修复的复验；需关闭游戏后重跑 build.ps1 安装——Host.dll 被运行中的游戏映射锁定）**：⓪ 进存档日志应出现 `game session start — re-registered N mod listener(s)` + `GameStart dispatched to mods`（N≈14：batch4+powerlink2+finder3+autopriority5），且存档内 Toast「自动任务优先级:开…」出现；① 右侧两枚 HUD 按钮出图标（延迟 SetIcon 路径）；② **小人升级后无需任何手动切换**，~0.5s 内自动加点（日志 `run(event): spend[...]`；❤项优先、其次最高属性；全满时点数保留）；③ 加点/新成员/死亡后优先级格子自动变 高/中/次 三档；④ Ctrl+3/Ctrl+4 与按钮切换，手动开有汇总 Toast、关有"保留现状"提示且落盘；⑤ 双开关全关→零写入（手动调的优先级不再被覆盖，日志无 `run(event)` 行）；⑥ 事件风暴无卡顿、无日志刷屏（防抖合并 + 变化才写）；⑦ 退存档再进第二个存档：订阅恢复（再次 re-registered 行）、按钮图标重设、batchmanager 图标同样恢复。若异常：先找 `run(manual)/run(event)` 行，再开 `verbose` 看逐条 `priority/spend` 明细。
- [ ] host 事件层修复的连带复验：batchmanager/powerlink/buildingfinder 的 `Events.On`（Buildable 四事件）此前进存档即失效、现恢复——确认 batchmanager 开着窗建造/拆除时列表自动刷新、powerlink 自动模式建造后 3s 触发连网（此前只有 10s 心跳兜底）。
- [ ] EnergyPlanner 任何改动必须跑 `dotnet run --project ModKit\tools\EnergyPlanner.Tests`（13 项）。
- [ ] **PowerLink 硬崩溃根因未定位**：用户「自动连网开+新建电池」时闪退过一次（无托管异常/无 WER/无转储）。已加固=分帧执行+`Mods\flotsam.powerlink\trace.log` 逐条落盘。**若复发：先读 trace.log 尾部**——最后一行 `pre connect X <-> Y` 即肇事对；无进行中动作则与连网无关（转查其他 mod/游戏本身）。
- [ ] BatchManager「全选不生效」用户报告一次、日志零痕迹，已加固观测（Tick 异常必进 `panel tick failed`、四个选择按钮点击必留统计行、空右栏 Toast）。**若复发：读 `selectall:`/`panel tick failed` 行定位**（点击没到 vs 列表为空 vs Refresh 抛错三种情况日志形态不同）。
- [ ] PowerLink 连接器 >500 时的空间分桶未做（当前 O(n²)，大图卡顿再优化，已有 Warn 提示）。
- [ ] BatchManager 右栏 `maxRows`(400) 截断为权宜；型号/实例极多时可考虑虚拟列表。
- [ ] **gameplaytweaks 进存档实测**（已验证"加载 + 7/7 补丁解析 + 0 报错"，用户已确认滑块可拖动且生效）：① 数值框**键入**（回车/失焦生效、非法输入回退）与 **5% 档位吸附**（拖动/键入都吸附）；② 重量能耗↓/载重上限↑（超重能否继续建）/发电效率↑（入网电量）/美观四项（城镇美观分与居民士气）是否如期变化；③ 总开关关→全部回原版；④ 恢复默认；⑤ 窗口缩放/拖动/记忆、隐藏帮助无空白带；⑥ 键入时快捷键（F7/WASD 等）应被 `UIState.Typing` 屏蔽。
- [ ] 箭头尺寸现为 1.5×（18–33px），如用户仍嫌小/大再调 `MiniMapView.UpdateTownheartMarker` 的 `marker`。
- [ ] F10 管理器（IMGUI）目前只可拖拽、不可缩放/记忆尺寸之外的布局优化未做。
- [ ] 原生粗体字重若游戏更新改名，需按日志 `native skin:` 的字体清单更新 `NativeSkin` 偏好表。
- [ ] `RecipeToggleBadge` 仅展示「缺 N」，未做点击行为（有意为之，避免与配方选择冲突）。
