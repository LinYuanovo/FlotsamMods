# Flotsam ModKit 使用说明

一套自研的 Flotsam（1.0.1f7 / Unity 6000.3.16f1 / Mono）mod 框架 + 七个功能模组。
地基是 **BepInEx 5.4.23.5 + HarmonyX**，上层是自研的 `FlotsamModKit` 管理器。

> 本文件对应 **v2**：四个模组在首轮实测后全部按根因重做（改动理由见 `设计与实现方案.md` §6）。

---

## 1. 当前状态（实测）

| 项目 | 状态 |
|---|---|
| 编译 | 7 个项目全部 `0 error / 0 warning`（离线构建，无需联网） |
| 注入 | ✅ 已实测：Doorstop/winhttp 注入 `Flotsam.exe`，BepInEx 5.4.23.5 在 `Unity 6000.3.16.10841891` 下正常启动 |
| 宿主 | ✅ 已实测：发现 4 个模组，`4/4 mod(s) enabled` |
| Harmony 补丁 | ✅ 已实测：**6 个目标全部解析成功**（见下方日志） |
| 运行时玩法 | ⚠️ **未实测**：移动/分类列表/排产按钮/小地图观感需要你进存档确认（见 §8） |

`BepInEx/LogOutput.log` 关键片段（v2）：

```
[flotsam.keyboardmove]   keybinds W/S/A/D, toggle F8, mode headless, speed x1, commit every 0.15s
[flotsam.keyboardmove]   postfix FlotsamInputManager.GetMovementInput <- AfterGetMovementInput
[flotsam.keyboardmove]   prefix  WorldMap.Close <- BeforeWorldMapClose
[flotsam.materialhelper] postfix ProductionPanelRecipeToggle.Initialize <- AfterToggleInitialize
[flotsam.materialhelper] postfix ProductionPanelRecipeToggle.UpdateState <- AfterToggleState
[modkit] 4/4 mod(s) enabled — manager key F10
```

---

## 2. 文件布局

```
F:\Game\Flotsam\
├─ winhttp.dll                     ← Doorstop 4.5 代理（BepInEx 注入入口）
├─ doorstop_config.ini
├─ .doorstop_version
├─ BepInEx\
│  ├─ core\                        ← BepInEx 5.4.23.5（含 0Harmony / HarmonyX 2.9.0）
│  ├─ config\BepInEx.cfg
│  ├─ LogOutput.log                ← 完整日志（管理器窗口只显示最近 400 行）
│  └─ plugins\FlotsamModKit\
│     ├─ FlotsamModKit.Host.dll           ← 唯一的 BepInEx 插件
│     ├─ Flotsam.ModKit.Abstractions.dll  ← 标准接口（mod 共用，只此一份）
│     └─ Flotsam.ModKit.Game.dll          ← 游戏耦合门面（GameApi / GamePatches / …）
├─ Mods\
│  ├─ ModKit.json                  ← 管理器状态（各 mod 的启用开关、管理窗口按键）
│  ├─ flotsam.keyboardmove\   { mod.json, FlotsamMod.KeyboardMove.dll, keybinds.json, config.json }
│  ├─ flotsam.buildingfinder\ { … }
│  ├─ flotsam.materialhelper\ { … }
│  ├─ flotsam.minimap\        { … }
│  ├─ flotsam.gameplaytweaks\ { … }
│  ├─ flotsam.batchmanager\   { … }
│  ├─ flotsam.powerlink\      { … }
│  └─ .trash\<id>-<时间戳>\      ← 卸载回收站（可手动还原）
└─ ModKit\
   ├─ build.ps1                   ← 一键编译 + 安装
   ├─ 使用说明.md                  ← 本文件
   ├─ 设计与实现方案.md             ← 调研结论 + 架构 + 实现依据 + v2 修复记录
   └─ src\                        ← 全部源码
```

---

## 3. 安装 / 卸载 / 更新

```powershell
cd F:\Game\Flotsam\ModKit
.\build.ps1                 # 编译并安装到 F:\Game\Flotsam
.\build.ps1 -NoInstall      # 只编译
```

**打包分发给其他玩家**（对方无需任何编译环境）：

```powershell
cd F:\Game\Flotsam\ModKit
.\package.ps1            # 打包当前已安装状态 → dist\FlotsamModKit-<版本-日期>.zip（约 0.9MB）
.\package.ps1 -Rebuild   # 先重新编译+安装再打包（需先关游戏）
```

玩家把压缩包里的**全部内容**解压到自己的游戏根目录（`Flotsam.exe` 所在文件夹），正常启动游戏即可；
包内自带 BepInEx 加载器、全部模组与一份自动生成的 `安装说明.txt`（模组清单/默认按键/卸载/常见问题）。
个人设置（`Mods\**\config.json`、`keybinds.json`、`ModKit.json`）不进包——对方首次运行时按默认值自动生成，全部模组默认启用。

**卸载**

- 只删模组：删除 `Mods\flotsam.xxx\` 目录，或在管理器窗口点"卸载"（进 `.trash` 可找回）。
- 完全移除 ModKit：删除 `BepInEx\`、`Mods\`、`winhttp.dll`、`doorstop_config.ini`、`.doorstop_version`。
  这六项是**全部**新增内容——没有修改任何游戏本体文件。

---

## 4. 管理器窗口（默认 **F10**）

- **模组**：列出所有 mod（名称/版本/状态/补丁数）。`启用`/`禁用`立即生效（禁用会回滚该 mod 的全部补丁、订阅与 UI）；`卸载`需先禁用；`重载`重新扫描 `Mods\`（新装的 mod 无需重启游戏）。
- **按键**：所有 mod 注册的按键，点"改键"后按任意键改绑，自动存盘（Esc 取消）。
- **日志**：最近 400 行，按 mod 前缀区分；完整日志在 `BepInEx\LogOutput.log`。

改管理窗口按键：`Mods\ModKit.json` 的 `"managerKey": "F10"`。

> 诚实边界：Unity 的 Mono **无法卸载已加载的程序集**。"禁用"= 行为立即回滚，但代码仍在内存里；彻底释放内存需重启游戏。

---

## 5. 模组一览

### 5.1 键盘移动（`flotsam.keyboardmove`）

- **做什么**：世界地图**关闭**时，直接用键盘驾驶镇心（船）。
- **按键**：默认 `W/S/A/D`（自动沿用你已改过的原生镇心移动绑定），`F8` 暂停/启用。可在管理器改键。
- **原理（v2 修正）**：上一版有两处根本性错误——
  1. **一次 `MovementEvent.DispatchTownheartMove` 不是"移动原语"**：它唯一的监听者会重排瓦片内容，并连带**重建整块地形网格、清空寻路图、把每个漂流者 warp 一次**。每帧调用 = 持续重生成 → 你看到的"别的视角乱了"。
  2. `World.RepositionTownheart` **只接受"已生成瓦片范围内"的位置**，而瓦片只由 `WorldMap.LateUpdate` 生成（地图打开时才跑）→ 出界即被拒绝并抛异常 → "并没有真实移动"。
  v2 改为**后台地图（headless）**：把地图层级保持**激活**（让游戏自己的移动器跑物理、结算能量、继续生成瓦片），只**关闭它的画面**（地图相机 + 地图画布）；按键通过游戏自己的输入汇聚点 `FlotsamInputManager.GetMovementInput` 注入；世界位置按 **`commitInterval`（默认 0.15s）节流提交**，而不是每帧。
- **注意**：在世界视图里，**镇心本身不会位移**——这是原版的构造（世界内容是"城镇相对系"，`ReturnLocalToWorldPosition` / `RepositionRelativeToTownheart`），所以正确观感是**世界从船边滑过**。打开地图后位置与朝向一致。
- 进入后台模式后按地图键：先自动退出后台模式，再由原版正常打开地图（避免原版用未初始化的记账数据关地图）。
- **配置** `Mods\flotsam.keyboardmove\config.json`：

  | 键 | 默认 | 说明 |
  |---|---|---|
  | `mode` | `headless` | `headless`=后台地图（推荐）；`commit`=旧式直接提交（仅在 headless 不可用时） |
  | `speedMultiplier` | `1` | 速度倍率（0.1~8） |
  | `commitInterval` | `0.15` | 世界位置提交间隔（秒）。越小越顺滑但重生成越频繁；卡顿就调大 |
  | `hideMapPresentation` | `1` | 关闭地图相机与地图画布 |
  | `verbose` | `1` | 每次提交打印 `commit d=… `，方便确认是否真的在移动 |

### 5.2 建筑快速查找（`flotsam.buildingfinder`）

- **做什么**：屏幕右侧常驻按钮「建筑总览」（或按 `B`）打开：
  - **左侧 = 游戏原生的分类**：使用原生分类**图标**与**分类配色**，并显示每类数量；点击即筛选；
  - **右侧 = 建筑列表**：按游戏自己的排序规则（`SortBuildableList`）显示 图标 / 名称 / 建造中 / 故障数（`⚠ n`，来自原生 `PopulateMalfunctions`）；
  - **"全部"视图按分类分组**，每类一个彩色表头，不再是一坨平铺；
  - 搜索框支持 **名称 / I2 词条 / 分类名**（中英文均可）。
- **v2 修正**：分类以前在 `OnEnable` 里读取，而那时游戏还没进场景（`GameManager.Settings` 为空）→ 分类列表是空的，所以"全挤在一起"。现在改为**懒加载**：进存档后或首次打开面板时再读取并构建分类。
- **按钮与窗口可拖动并记忆**：HUD 按钮按住即可拖到任意位置，窗口可拖标题栏；位置写入该 mod 的 `config.json`。
- **配置**：`zoomLevel`(0.6)、`hideAfterFocus`(1)、`includeUnfinished`(1)、`groupAllByCategory`(1)、`maxRows`(400)、`draggableButton`(1)、`buttonX/buttonY`、`windowX/windowY`、`lastCategory`。

### 5.3 材料缺失辅助（`flotsam.materialhelper`）

- **做什么**：工坊缺料时一键去生产，**两个可靠入口**：
  1. **快捷键 `G`**（在工坊面板里）：对当前选中配方的缺料项安排生产；
  2. **配方列表每一行右侧的 `→生产 n` 按钮**（常驻、可点，n = 可生产的缺料项数量）。
- 动作：自动找到能生产该材料的**已完工建筑** → 相机飞过去 + 打开其生产面板 + 选中配方 + 数量 +1。`Alt`（配合点击/按键）= 填满队列，`Shift` = 连续生产。
- **为什么不用点缺料行（v2 修正）**：缺料行上的提示是**跟随鼠标的悬浮框**——`TooltipPanel` 每帧把浮层贴到光标右下 (15,5) 处，而且每个材料行在 `Awake` 都被挂上了 `ItemTooltip`。所以鼠标一靠近就出现、一移开就消失，**行本身永远点不到**；上一版的小标记还是 `raycastTarget=false`（根本不接收点击）。现在改挂在**常驻的配方行**上。
- 也**不能**用故障/警告图标当触发：那东西在"该配方已有排队"时会被抑制，而且每次排产尝试都会清掉再加，是闪烁的。
- **范围**：Workshop 类建筑（农场原版不显示缺料高亮）。
- **配置**：`zoomLevel`(0.6)、`showBadge`(1)、`startAmount`(1)、`altClickFillsQueue`(1)、`shiftClickContinuous`(1)。
- **为什么缺料也能排**：游戏自己的 `QueueRecipe` 在材料不足时干净地返回 false，而 `GameManager.LateUpdate` 每帧泵 `Producer.UpdateQueuedProducer()`——材料一到就自动预留并开工，不需要重试轮询。

### 5.4 可缩放小地图（`flotsam.minimap`）

- **做什么**（右侧，`M` 开关）：
  - **滚轮在光标处自由缩放**（0.2×~20×，锚点补偿，不会跑偏）；**拖拽平移**；**双击居中**；「适应世界」；**跟随镇心**开关（拖拽会自动关掉跟随）。
  - **底图 = 游戏真实的迷雾遮罩**（`WorldTile.FogOfWarAlphas`，逐顶点字节，代码里公开可读）+ 瓦片海域轮廓；
  - **标记 = 游戏自己的图标**（`ISpawner.Icon`：地标 / 兴趣点 / 漂浮垃圾），以 UGUI 图层绘制 —— 任意缩放下都清晰，未侦察的图标半透明；
  - 底部图例行显示各类数量；镇心用箭头显示**朝向**。
- **为什么不能直接复用原生地图**：原生地图瓦片在地图关闭后确实还活着，但挂在**被停用的父级**之下（`AddWorldTile` 用的是 prefab 的 parent），所以单独 `SetActive(true)` **什么都不渲染**；整棵重新激活又会连带它的移动器在暂停时驱动全局物理。因此改为"**用游戏自己的数据与图标重新合成**"。
- **配置** `Mods\flotsam.minimap\config.json`：

  | 键 | 默认 | 说明 |
  |---|---|---|
  | `textureSize` | `384` | 底图分辨率（128~1024） |
  | `refreshHz` | `8` | 重绘频率 |
  | `zoom` / `followTownheart` / `visible` | `1` / `1` / `1` | 初始缩放 / 跟随 / 默认显示 |
  | `showFog` | `1` | 是否绘制真实迷雾 |
  | `fogInvert` | `0` | **迷雾方向**。代码里无法判定"1=雾"还是"1=可见"；如果看起来该亮的地方黑、该黑的地方亮，把它改成 `1` |
  | `showIcons` / `iconSize` / `iconMinAlpha` | `1` / `20` / `0.45` | 图标开关 / 大小 / 未侦察时的透明度 |
   | `draggableButton` / `buttonX` / `buttonY` / `windowX` / `windowY` | — | 按钮与窗口位置（拖动后自动写入） |

### 5.5 游戏性调整（`flotsam.gameplaytweaks`）

- **做什么**：一个原生皮肤的滑块面板，把三项平衡改成"原版的百分比"，**100% = 原版**，即时生效并记忆。每项**既可拖动滑块、也可在右侧数值框直接键入数字**（回车或点别处生效），**档位 5%**（拖动/键入都吸附到 5 的倍数）。按 `F7` 或点左上角「游戏性调整」按钮打开；窗口可拖动、可缩放、位置尺寸自动记忆；顶部有「总开关」（关掉=全部回原版）与「恢复默认」。键入数字时游戏进入输入态，`F7`/`WASD` 等快捷键不会误触。
- **九个滑块**（分区）：
  - **重量**
    - `重量能耗影响`（0–300%）：城镇越重、移动越费电（本作重量**不降速**，只涨每单位距离的能量消耗）。**0% = 重量不再增加能耗**，100% = 原版，200% = 重量惩罚翻倍。
    - `载重上限倍率`（100–1000%）：抬高牵引/载重上限。**超重建不了新建筑**时调高它即可继续建（不改城镇实际重量，故与上一项互不影响）。
  - **发电**
    - `发电效率`（0–500%）：**所有发电机**（风/水被动、燃料、人力）的产出统一放大。
  - **小人**
    - `整体执行效率`（25–500%）：小人**干活与走路一起**变快/变慢（排产/建造/研究/发电等所有工作速度 + 移动速度）。100% = 原版；下限 25% 防止小人"冻住"。
  - **船只**
    - `移动速度(打捞/渔船)`（25–500%）：打捞船/渔船的**航行速度**。船只速度与小人的执行效率**无关**（船用自己的固定船速），所以"小人调快了船还慢"时调这一项。100% = 原版。
  - **美观度**（两层，各含正/负）
    - `负面建筑贡献`（0–200%）/ `正面建筑贡献`（0–500%）：缩放每栋建筑的美观分（负分建筑更温和、正分装饰更给力），直接改变城镇总美观分。
    - `负面士气修正`（0–200%）/ `正面士气修正`（0–500%）：缩放美观分对**居民士气**的正/负作用。
- **配置** `Mods\flotsam.gameplaytweaks\config.json`：`active`(1)、`weightEnergyPct`(100)、`tugCapacityPct`(100)、`generatorPct`(100)、`agentEfficiencyPct`(100)、`boatSpeedPct`(100)、`beautyNegBuildPct`(100)、`beautyPosBuildPct`(100)、`beautyNegMoralePct`(100)、`beautyPosMoralePct`(100)、`showPanel`(0)、`verbose`(0)、`button.*` / `panel.*`（位置尺寸，拖动后自动写入）。
- **已知边界**：重量分档面板显示的"eels/单位"、发电总览里每台机器的速率、建筑美观 tooltip 可能仍显示**基础值**，但**实际结算**（扣电、入网电量、城镇美观分与士气）按缩放走。

### 5.6 批量管理（`flotsam.batchmanager`）

- **做什么**：按**建筑型号**批量勾选、批量操作。`Ctrl+1` 或右侧 HUD「批量管理」按钮开关窗口。
  - **左栏**：分类默认收起（点分类头展开/收起，搜索时自动全展开）；顶部固定「全部建筑」。型号行带状态摘要 `可升 X/Y`（X=当前材料足够可立即升级，Y=该型号有升级路径的总数；判定=游戏原生 `CanUpgrade`：已建成+科技解锁+社区材料足够）及 拆/升中、建造中、停用计数。
  - **右栏**：该型号全部实例，每行 = 勾选框（原生开/关图标）+ 图标 + 名称与状态 + ⚠故障数 + [定位]。**满足升级条件（材料足够）的行名为绿色**。搜索框过滤型号；行数超 `maxRows`(400) 截断并提示。
  - **工具条**：**选可升级**（一键勾选当前右栏所有材料足够的可升级建筑）/ 全选 / 反选 / 清空（作用于当前右栏）、已选计数。
  - **操作栏**：批量拆除（红）、取消拆除、批量升级、取消升级、启用、停用。每座建筑先做与原生按钮相同的预检，不满足的跳过并在 Toast 汇总原因（如「已下令拆除 8 座，跳过 2 座（1 正在搬料、1 资源不足/未解锁）」）。拆除/升级仍是派小人执行、材料返还/预留，可用「取消拆除/取消升级」撤回。
  - **二次确认**：勾选数 ≥ `confirmThreshold`(5) 时点「批量拆除」，操作栏变红色确认条（10 秒不确认自动取消；期间做任何其他操作也会取消）。
- **配置** `Mods\flotsam.batchmanager\config.json`：`highlightChecked`(1)、`confirmThreshold`(5)、`sortByDistance`(0，按距镇心远近排序实例)、`includeUnfinished`(1)、`hideAfterFocus`(0)、`zoomLevel`(0.6)、`maxRows`(400)、`verbose`(0，开=逐条日志)、`window.*`、`button.*`。

### 5.7 一键电网（`flotsam.powerlink`）

- **做什么**：`Ctrl+2` 或 HUD「连电网」按钮，一键把所有**够得着**的未供电建筑/电线杆按**最短总电缆**接入带电网络（可经你已建的电线杆接力），完全遵循游戏自己的连线规则（线长上限、接口空槽），连出的电缆与手动连接**完全等价并随存档保存**，且不花任何材料（原版连线本就免费）。
  - **整理**：一键时会顺带检查既有布线是否最优；只有明显更短（收益 ≥ `optimizeGainPct`，默认 10%）才会先拆后连重排（Toast 会报「整理移除 X 根…总长 -Y%」），微小差异绝不动你手排的线。整理**只由手动一键触发**，自动模式只做增量。
  - **智能互济**：两个独立带电电网之间，只在「一边缺电（效率<100%）× 另一边有富余」时才合并互济；都富余或都缺电不合并（`mergePoweredGrids` 可关）。
  - **自动连网**：HUD「自动连网:开/关」按钮切换。开启后新建筑建成约 3 秒内自动接线，另有心跳兜底；无变化时零打扰（不弹 Toast 不刷日志）。
  - **够不着的建筑**：超出线长又无杆接力的会计入「N 座超距无法连接（需要电线杆）」——mod **不会**替你建电线杆（要花资源），只是告诉你差在哪。
  - `connectPolesToGrid`(1)：够得着的已建电线杆本身也接入电网（顺带消除杆的「未连线」红字）；改 0 则杆只做接力、不为孤立备用杆拉线。
- **配置** `Mods\flotsam.powerlink\config.json`：`autoMode`(0)、`autoIntervalSec`(10)、`optimizeExisting`(1)、`optimizeGainPct`(10)、`connectPolesToGrid`(1)、`mergePoweredGrids`(1)、`verbose`(0，开=快照统计+逐边 link/drop/merge 日志)、`button.*`、`autobutton.*`。

### 5.8 性能优化与探针（`flotsam.perfboost`）

- **做什么**：大城镇掉帧的体检仪 + 优化包。按 `F9`（或左上 HUD「性能面板」按钮）打开面板：FPS（均值 + 1% 低帧）/ 帧毫秒数、**主线程/渲染线程/GPU 帧耗时**、**渲染统计**（DrawCall/批次/三角形/渲染纹理数与体积）、**逐相机渲染耗时**（每台相机一行）、**脚本热点**（每个游戏子系统的每帧毫秒 + 单次峰值，自建秒表实测）、**内存**（堆/预留/Mono），底部是建筑/小人/已优化对象计数。面板数据每 10 秒写一行日志（`BepInEx\LogOutput.log`），可整段发回用于分析。
- **为什么有效**：本游戏渲染**确实跑在独显上**（Player.log 可见 RTX 4070 SUPER）；建筑多了卡是 **CPU 主线程瓶颈**（任务管理器里 GPU 占用低/显示 0% 是因为显卡在等 CPU）。所以优化方向是砍掉主线程的浪费，而不是"让游戏用显卡"。
- **内置优化项**（`Mods\flotsam.perfboost\config.json`，每项独立开关，禁用 mod 即全部回原版）：

  | 键 | 默认 | 说明 |
  |---|---|---|
  | `disableCtaa` | `1` | 关闭挂在 3 台相机上的 CTAA 空转：游戏是 URP 渲染，CTAA 的抗锯齿本就不执行，只剩每帧投影抖动与强制运动向量（多画一遍运动物体）——纯浪费 |
  | `suppressLogSpam` | `1` | 掐掉水花粒子销毁/PrefabPool 销毁的日志刷屏；普通日志不再采集栈回溯（报错仍保留） |
  | `shadowDistance` | `100` | 阴影距离**上限**（原版约 240，城镇尺度看不出差别；游戏缩放时会自己调阴影远近，此项钳住最大值，缩放逻辑不变）；`0`=阴影全关（阴影会把每个建筑再画一遍，是 DrawCall 最大的倍增器） |
  | `srpBatcher` | `1` | **SRP Batcher**（URP 的 CPU 渲染提交加速，能省一大截主线程；不兼容的着色器自动回落，安全）。游戏原版若没开会强制打开；`0`=不动。开关状态见日志 `pipeline:` 行与面板「SRP批」 |
  | `shadowCascades` | `2` | 阴影级联数（原版是 **4**——每帧把全部阴影投射体重画 4 遍；配合阴影上限 100，2 级联画质几乎无差、阴影绘制量减半。`1`=最大省（可再减半）、`4`=回原版、`0`=不动）。原版值见日志 `pipeline:` 行 |
  | `shadowmapRes` | `2048` | 主光阴影图分辨率（原版 **4096**；2048 = 阴影填充开销降到 1/4，配合级联 2 阴影总开销约原版 1/8）。`0`=不动、`4096`=原版 |
  | `forceProfiler` | `1` | 强制开启 Unity 性能计数引擎（让 F9 面板能读到 DrawCall 等数据；若发现它本身影响帧率可关） |
  | `vSync` | `0` | 垂直同步（原版开=1）。已实测它在等刷新（每帧约多等一个刷新周期）；本作画面移动慢，关掉撕裂几乎看不出、帧率更顺。`-1`=不动 |
  | `targetFps` | `0` | 帧率上限（`0`=不限制；介意功耗/噪音可设 60 或 120） |
  | `cullOffscreenAnimators` | `1` | 视野外的建筑/小人动画只推进状态、不写骨骼（风扇等出屏动画不再耗 CPU） |
  | `animatorFreeze` | `1` | **离屏动画冻结**（视野外的动画**完全停止**——回屏自动恢复。若发现某建筑动画状态异常，关掉它即回到上一档行为） |
  | `animatorFixedUpdate` | `1` | **装饰动画半频**（摆动类动画器改在 50Hz 物理步里求值而不是每帧——装饰/作物摆动视觉无差，动画求值开销约减半。角色动画若出现轻微顿挫可关） |
  | `particleRateScale` | `0.5` | 水花等池化粒子的发射密度倍率；`1`=不动 |
  | `lodBias` | `1` | LOD 偏置（0.7 = 更远就切低模）；`1`=不动 |
  | `renderScale` | `1` | URP 渲染分辨率比例（0.85 = 按 85% 渲染再放大，GPU 大减负）；`1`=不动 |
  | `fixedDeltaMs` | `0` | 实验项：物理步长（默认 20ms；改 33 = 物理开销减 1/3，手感略糙）；`0`=不动 |
  | `hotspots` | `1` | 脚本热点秒表（14 个游戏管理器的每帧耗时+峰值，F9 面板与日志可见）；关=不挂补丁 |
  | `showOverlay` / `logIntervalSec` | `1` / `10` | 面板默认显示 / 日志快照间隔（秒） |

- **注意**：「GPU帧」接近 0 说明显卡闲着（CPU 瓶颈的直接证据）；明显变大才是显卡瓶颈（那时再考虑 `renderScale`）。**双击面板标题栏可复位窗口尺寸**（面板行数较多，旧窗口尺寸会截断内容）。
- **D3D12 渲染后端实验（结论：无收益）**：`启动-D3D12.bat` 可用 D3D12 后端启动（Player.log 显示 `Direct3D 12`），实测帧率与 D3D11 基本相同（32~44 vs 34~42）。**建议用正常方式启动（D3D11）**；bat 保留作对照，回滚=正常双击 Flotsam.exe，无任何文件被修改。

---

## 6. 配置与状态文件速查

| 文件 | 作用 |
|---|---|
| `Mods\ModKit.json` | 管理器状态：各 mod 的 `enabled.<id>`、`managerKey` |
| `Mods\<id>\mod.json` | 模组清单（别手改 entryType） |
| `Mods\<id>\keybinds.json` | 该模组的按键绑定（改键后自动写入） |
| `Mods\<id>\config.json` | 该模组的配置项 |
| `BepInEx\config\BepInEx.cfg` | BepInEx 自身设置 |
| `BepInEx\LogOutput.log` | 完整运行日志 |

---

## 7. 开发一个新 Mod

1. 新建类库，引用（**`Private=false`**，不要把共享 DLL 拷进 mod 目录）：
   `Flotsam.ModKit.Abstractions.dll`、`Flotsam.ModKit.Game.dll`（外加 `Assembly-CSharp.dll` 等游戏程序集与 BepInEx/0Harmony）。
   目标框架 **netstandard2.1**（Unity 6 的程序集是 2.1，用 2.0 会报 `CS1705`）。最省事：复制 `src\mods\FlotsamMod.MiniMap\` 改名。
2. 实现入口：

```csharp
public sealed class MyMod : FlotsamModBase
{
    public override void OnLoad(IModContext ctx)      // 注册按键/配置（不碰游戏对象）
    {
        base.OnLoad(ctx);
        Keybinds.Register("mymod.toggle", KeyCode.F7, "开关");
        Log.Info("loaded");
    }

    public override void OnEnable()                    // 打补丁 / 订阅事件 / 建 UI
    {
        // 用 GamePatches 而不是 AccessTools.Method：后者在同名方法（含基类）存在时
        // 会抛 AmbiguousMatchException，一次就会让整个 mod 被标记为加载失败。
        var target = GamePatches.MethodByName(typeof(SomeGameClass), "SomeMethod");
        if (target == null) { Log.Error("目标未找到: " + GamePatches.Describe(typeof(SomeGameClass))); return; }
        Patches.Postfix(target, AccessTools.Method(typeof(MyMod), nameof(After)));
        Events.On("GameStart", _ => Ui.Toast("我的 mod 生效了", ToastKind.Success));
    }

    public override void OnDisable() { }               // 补丁/事件由宿主自动回滚；自建物体要销毁
    public static void After(object __instance) { }
}
```

3. 写 `mod.json`（示例见 `Mods\flotsam.minimap\mod.json`）：`id / name / version / apiVersion / entryAssembly / entryType / type(code) / keybinds[]`。
4. `.\build.ps1` 会装进 `Mods\<id>\`；游戏里 F10 →「重载」即可看到，无需重启。

**标准接口**（`IModContext`）：`Log`、`Config`、`Keybinds`、`Patches`（每 mod 独立 Harmony，禁用即整体回滚）、`Events`（自动退订）、`Ui`（Overlay/HUD 按钮/Toast）、`Control`。
**游戏侧门面**（`FlotsamModKit.Game`）：`GameApi`、`GamePatches`（安全目标查找）、`GameBuildings`、`GameProduction`、`GameMovement`、`GameWorld`、`GameKeys`、`GameUi`。

---

## 8. 请你实测的清单与已知限制

**最需要你确认的五点**（这次只验证到"加载 + 6 个补丁解析成功"）：

1. **键盘移动**：在地图关闭时按 `W/S/A/D`，看**世界是否从船边滑过**（镇心本身不位移是正常的），并看日志里是否持续出现 `commit d=…`。若日志没有 commit，请把 `verbose` 保持 `1` 并把日志发我。
2. **后台模式的副作用**：地图 UI/相机是否有残留；暂停、打字、打开其他面板时是否停手；按地图键是否正常进入真正的地图。
3. **建筑总览**：左侧是否出现带图标的分类、数量是否正确、"全部"是否按分类分组、按钮拖动后重启是否记住。
4. **`→生产 n` 按钮**：在工坊配方列表里是否可见、可点、点击后是否正确飞向生产者并排产。
5. **小地图迷雾方向**：若该亮处偏黑，把 `fogInvert` 改成 `1` 再看。
6. **游戏性调整**（`F7`）：面板 7 个滑块拖动是否顺滑、数值框是否跟随；**数值框直接键入数字**（回车/点别处生效、超范围自动钳制、非法输入回退）与 **5% 档位吸附**是否正常；键入时 `F7`/`WASD` 是否被屏蔽。分别验证——重量能耗调到 0% 后移动是否不再因重量掉电、载重上限调高后超重能否继续建、发电效率调高后入网电量是否上升、美观四项调整后城镇美观分与居民士气是否随之变化；总开关关掉是否整体回原版；窗口缩放/拖动/隐藏帮助后重启是否记住。
7. **批量管理**（`Ctrl+1`）：型号分组与数量是否和建筑总览对得上；勾选行世界中是否出现描边高亮、取消是否消失；`◀▶` 逐个跳转是否正确；勾 2-3 座「批量拆除」小人是否去拆且 Toast 汇总正确；勾 ≥5 座是否弹红色确认条（取消/10s 超时应无效化）；「取消拆除」能否撤回；批量升级资源不足时是否跳过并计入汇总；隐藏提示后操作栏下方是否无空白带；退存档再进窗口是否正常重建、无残留高亮。
8. **一键电网**（`Ctrl+2`）：有未供电建筑时一键是否全部接入（电缆出现、「未连接电网」红字消失）、Toast 数字是否对；再按一次应提示「电网已完整」；故意拉一根远线再一键，看是否按最短重排（收益 <10% 应不动）；「自动连网:开」后建一座新用电建筑是否几秒内自动接线；存档→读档后 mod 连的线是否保留；超距建筑是否只提示不强连。

**已知限制**：

- Mono 无法卸载程序集：禁用只回滚行为，内存需重启释放。
- 缺料行本身仍不可点（悬浮框机制所限），所以入口是快捷键 `G` 与配方行按钮。
- 农场（`Producer.Type.Farm`）不参与缺料辅助。
- 后台地图方案依赖"地图层级 + 私有 `_townheartPhysics` 字段"的现状；游戏大版本更新后可能需要同步更新 mod（失败会被管理器隔离并标红，不影响其他 mod）。
- 游戏本体是第三方整合包（含 Steam 模拟器、汉化改写过的 `resources.assets`），本套件**不做任何文件哈希校验**。
- `ModKit\src\_probe\` 是编译链路验证用的遗留工程，不参与安装，可删。